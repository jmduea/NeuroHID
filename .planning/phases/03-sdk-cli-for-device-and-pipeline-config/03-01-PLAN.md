---
phase: 03-sdk-cli-for-device-and-pipeline-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-sdk/src/lib.rs
  - crates/neurohid-sdk/src/device.rs
  - crates/neurohid/src/bin/neurohid.rs
  - crates/neurohid/src/cli.rs
  - crates/neurohid/Cargo.toml
autonomous: true
requirements:
  - COMP-01

must_haves:
  truths:
    - Developer can list discovered devices/streams via SDK API
    - Developer can list discovered devices/streams via CLI (e.g. neurohid device list)
    - Developer can connect to a stream by id or by criteria via SDK
    - Developer can connect to a stream by id or by criteria via CLI
    - Connection handle disconnects on drop (scoped lifecycle)
  artifacts:
    - path: crates/neurohid-sdk/src/device.rs
      provides: Public device discovery and connection API
      contains: list (or list_streams), connect_by_id, connect_by_criteria, optional listener, handle type
    - path: crates/neurohid/src/cli.rs
      provides: CLI dispatch and device subcommands
      contains: device list, device connect, --json, -v, -q, exit codes
  key_links:
    - from: neurohid-sdk device API
      to: neurohid-core RuntimeCommand / ControlCommand or neurohid-device DeviceProvider::discover()
      via: delegation to existing runtime or discovery-only path
    - from: neurohid binary
      to: CLI when first arg is device
      via: arg parse and run_cli vs run_hub
---

<objective>
Expose device discovery and connection via a stable SDK API and CLI so developers can list devices/streams and connect by id or criteria without the Hub.

Purpose: Satisfy COMP-01 (drive device discovery, connection, and stream selection via public SDK and/or CLI). Align with CONTEXT: list + connect-by-id and connect-by-criteria; ongoing discovery in background; optional listener plus current list for scripts; first match for criteria (order implementation-defined); multiple connections in scope; handle ref-counted or scoped (drop = disconnect).

Output: neurohid-sdk device module with list, connect_by_id, connect_by_criteria, optional change listener, and a ConnectionHandle that disconnects on drop; neurohid binary that runs CLI when first arg is `device` (or other subcommand), with `device list` and `device connect` subcommands per CONTEXT (default table, --json, -v/-q, exit codes, progress on stderr).
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-CONTEXT.md
@.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-RESEARCH.md
@crates/neurohid-core/src/runtime.rs
@crates/neurohid-types/src/control.rs
@crates/neurohid-ipc (send_control_request for CLI talking to running service)
@docs/crate-boundaries.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDK device discovery and connection API</name>
  <files>crates/neurohid-sdk/src/lib.rs, crates/neurohid-sdk/src/device.rs</files>
  <action>
Implement a public device API in neurohid-sdk that:
- Lists discovered streams: either from a runtime snapshot (RescanStreams then Snapshot, use snapshot.discovered_streams) or from a discovery-only path using DeviceProvider::discover() mapped to a stable id/name list. Expose at least one of: list attached to RuntimeHandle, or standalone list via Provider. Per CONTEXT: "ongoing discovery in background; SDK exposes updating list or notifies when devices appear/disappear; optional listener/callback plus current list for scripts."
- Connect by id: send ConnectStream { stream_id }; by criteria: list streams, take first match (e.g. first LSL or first matching type), then ConnectStream. Document that criteria order is implementation-defined.
- Optional listener/callback for device list changes (for reactive UIs); plus a way to get current list for scripts.
- Connection handle: type that holds stream_id and runtime/control reference; on Drop send DisconnectStream so drop = disconnect. Ref-counted or scoped per CONTEXT; explicit disconnect optional.
- Do not duplicate discovery logic: delegate to DeviceTask/Provider and RuntimeCommand/ControlCommand (see neurohid-core runtime.rs, neurohid-types control.rs). Re-export or wrap types from neurohid-types (DiscoveredStream, ControlSnapshot) as needed.
- Follow crates/AGENTS.md: Result/?, no unwrap in library paths; document public API in device.rs.
  </action>
  <verify>cargo check -p neurohid-sdk; cargo test -p neurohid-sdk; cargo clippy -p neurohid-sdk -- -D warnings</verify>
  <done>SDK exposes list (from snapshot or discover()), connect_by_id(stream_id), connect_by_criteria(predicate or type), optional listener; a handle type that disconnects on drop; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: CLI device subcommands under neurohid binary</name>
  <files>crates/neurohid/src/bin/neurohid.rs, crates/neurohid/src/cli.rs, crates/neurohid/Cargo.toml</files>
  <action>
Implement CLI mode for the neurohid binary and device subcommands per CONTEXT and RESEARCH:
- Entrypoint: In neurohid binary (crates/neurohid/src/bin/neurohid.rs), if first argument is a known subcommand (device, config, pipeline, control), run CLI instead of launching Hub. Otherwise launch Hub as today.
- Subcommands: Add `device list` and `device connect`. Use clap 4.4 derive; subcommand structure: neurohid device list [--json] [-v|--verbose] [-q|--quiet]; neurohid device connect [--device-id &lt;id&gt;] [--criteria &lt;type&gt;] [--config path] [--profile path]. Config file as main source; flags override (--config, --profile). When multiple devices and no --device-id, prompt to choose only when TTY and ambiguous; otherwise no prompts for scripts.
- Output: Default human-readable table for list; --json for scriptable output (compact one-line by default; optional --json pretty or -v for indented). Progress/spinner on stderr, result on stdout; -q suppresses progress.
- Exit codes: 0 = success; non-zero = failure. Document in code or small doc: e.g. 1 = generic, 2 = not found, 3 = config invalid. When --json, failures write a JSON object (e.g. to stderr) with code, message, optional details.
- Implementation: CLI can use neurohid-sdk device API for in-process list/connect, or neurohid-ipc to send RescanStreams then Snapshot and ConnectStream to a running service. Prefer one path (e.g. IPC when control endpoint configured, else in-process runtime) and document. Short one-line summary per subcommand in main help; full description in neurohid device --help.
  </action>
  <verify>cargo build -p neurohid; neurohid device --help; neurohid device list --help; neurohid device list -q (or with --json) runs and exits with 0 or documented non-zero; cargo clippy -p neurohid -- -D warnings</verify>
  <done>neurohid device list and neurohid device connect work; output and exit codes follow CONTEXT; help shows one-line summaries; scripts can use --json and explicit args without prompts.</done>
</task>

</tasks>

<verification>
- cargo check --workspace; cargo test --workspace; cargo clippy --workspace -- -D warnings
- neurohid device list (and with --json, -q) produces expected output and exit code
- neurohid device connect --device-id &lt;id&gt; (or --criteria) sends connect and completes with 0 or documented error code
- SDK device module is documented and handle drop triggers disconnect
</verification>

<success_criteria>
- COMP-01 satisfied: Developer can drive device discovery, connection, and stream selection via public SDK API (Rust) and CLI (neurohid device list, neurohid device connect)
- Must_haves truths and artifacts present; key links (SDK→runtime/Provider, CLI→SDK or IPC) implemented
</success_criteria>

<output>
After completion, create `.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-01-SUMMARY.md`
</output>
