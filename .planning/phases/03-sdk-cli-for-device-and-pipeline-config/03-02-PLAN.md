---
phase: 03-sdk-cli-for-device-and-pipeline-config
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/neurohid-storage/src/config.rs
  - crates/neurohid-storage/Cargo.toml
  - docs/formats/config-format.md
  - crates/neurohid-sdk/src/lib.rs
  - crates/neurohid-sdk/src/config.rs
  - crates/neurohid/src/bin/neurohid-service.rs
autonomous: true
requirements:
  - COMP-02
must_haves:
  truths:
    - Config file can be YAML or TOML with same schema and format_version
    - Pipeline/decoder and signal config scope is documented (DecoderConfig, SignalConfig fields)
    - Developer can load and save config via SDK (ConfigStore or thin wrapper)
    - Developer can validate config via CLI (neurohid config validate) with exit 0 when valid
    - Developer can show config via CLI (neurohid config show); pipeline run has dry-run/validate
  artifacts:
    - path: crates/neurohid-storage/src/config.rs
      provides: Config load/save with TOML and YAML
      contains: "yaml|serde_yaml|format"
    - path: docs/formats/config-format.md
      provides: Pipeline/decoder and signal scope documentation
      contains: "decoder|signal|DecoderConfig|SignalConfig"
    - path: crates/neurohid-sdk/src/config.rs
      provides: Public config load/save API
      min_lines: 30
  key_links:
    - from: ConfigStore load/save
      to: SystemConfig
      via: serde deserialize/serialize with format_version
      pattern: "format_version|SystemConfig"
    - from: CLI config validate
      to: ConfigStore::load
      via: load and return result
      pattern: "load|validate"
---

<objective>
Add YAML config support, document pipeline/decoder config scope, expose config API in the SDK, and add CLI subcommands for config and pipeline so developers can configure the signal pipeline and decoder via SDK/CLI and documented config format.

Purpose: Satisfy COMP-02 — developer can configure signal pipeline and decoder (e.g. model path, params) via SDK/CLI and documented config format.
Output: ConfigStore supports YAML and TOML; config-format.md documents decoder/signal scope; neurohid-sdk config API; neurohid-service Config and Pipeline subcommands (config set/validate/show, pipeline run --dry-run or validate).
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-CONTEXT.md
@.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-RESEARCH.md
@crates/neurohid-storage/src/config.rs
@crates/neurohid-types/src/config.rs
@docs/formats/config-format.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: YAML config support and pipeline/decoder docs</name>
  <files>crates/neurohid-storage/src/config.rs, crates/neurohid-storage/Cargo.toml, docs/formats/config-format.md</files>
  <action>
Add YAML support to ConfigStore and document pipeline/decoder scope per CONTEXT (support both YAML and TOML).

ConfigStore (neurohid-storage):
- Add dependency serde_yaml to crates/neurohid-storage/Cargo.toml.
- Detect format by path extension: .yaml or .yml => serde_yaml::from_str / serde_yaml::to_string; otherwise TOML. Use same SystemConfig schema and always set format_version when saving (existing behavior for TOML). Load: read file, then deserialize with toml or serde_yaml based on path extension. Save: serialize with same format as path extension; if path has no extension or unknown, default to TOML. Support loading from an explicit path (e.g. for --config path from CLI); when loading from DataPaths::config_file() keep current behavior (TOML path). Either add load_from_path(path) / save_to_path(path, config) or extend load/save to accept Option&lt;PathBuf&gt; override; when override is Some(p), use p and detect format from p. Do not hand-roll YAML parsing; use serde_yaml. Respect format_version and compatibility policy from config-format.md for both formats.

docs/formats/config-format.md:
- Add a section documenting pipeline/decoder and signal config scope: list DecoderConfig fields (model_path, online_learning_enabled, learning_rate, gamma, gae_lambda, update_frequency_steps, batch_size, entropy_coef, value_coef, max_grad_norm) and SignalConfig fields (buffer_size_samples, notch_filter_enabled/hz, bandpass_*_hz, feature_window_ms, feature_step_ms, artifact_rejection_enabled, artifact_threshold_uv) as the scope for "signal pipeline and decoder" configuration. State that YAML and TOML share the same schema and format_version; readers support both based on file extension.
  </action>
  <verify>cargo test -p neurohid-storage; add a test that saves/loads a .yaml config and roundtrips format_version and decoder/signal fields. Check config-format.md contains decoder and signal scope.</verify>
  <done>ConfigStore can load and save YAML (by path extension); config-format.md documents decoder and signal scope and YAML/TOML parity.</done>
</task>

<task type="auto">
  <name>Task 2: SDK config API</name>
  <files>crates/neurohid-sdk/src/config.rs, crates/neurohid-sdk/src/lib.rs</files>
  <action>
Expose config load/save via neurohid-sdk so developers can get/set config programmatically. Add a config module (or re-export with thin wrapper) that exposes ConfigStore or equivalent: e.g. load(config_path: Option&lt;PathBuf&gt;) and save(config_path: Option&lt;PathBuf&gt;, config: &amp;SystemConfig). SDK can re-export ConfigStore from neurohid-storage when feature "storage" is enabled and add a small convenience API if needed (e.g. load from default path when None). Use neurohid_types::config::SystemConfig. Follow crate boundaries: config types stay in neurohid-types; storage in neurohid-storage; SDK re-exports or wraps for public API. Document in module doc that config format is versioned and YAML/TOML are supported.
  </action>
  <verify>cargo check -p neurohid-sdk --features storage; cargo test -p neurohid-sdk --features storage. Public API allows load and save of SystemConfig.</verify>
  <done>SDK exposes config load/save (via ConfigStore or wrapper); developers can get/set config from Rust code.</done>
</task>

<task type="auto">
  <name>Task 3: CLI config and pipeline subcommands</name>
  <files>crates/neurohid/src/bin/neurohid-service.rs</files>
  <action>
Add Config and Pipeline subcommands to neurohid-service per CONTEXT.

Config:
- ConfigCommandCli: Show (optional --config, --profile; print current config to stdout, human or --json), Validate (--config path; load config and exit 0 if valid, non-zero if invalid; when --json, write JSON error object to stderr on failure), Set (optional key/value or path to set specific fields; can be minimal for this phase: e.g. validate + show only if set is deferred). Implement at least Show and Validate. Use ConfigStore::load with path from --config when provided; otherwise DataPaths::config_file(). Exit code 3 for config invalid when validation fails. When --json and failure, output machine-readable error to stderr (code, message, optional details).

Pipeline:
- PipelineCommandCli: Run with optional --dry-run (validate config and do not start runtime; exit 0 if valid), or a dedicated Validate that mirrors config validate for pipeline-specific checks if any. For phase scope, pipeline run --dry-run can call ConfigStore::load and optionally check decoder path exists; exit 0 if valid.

Global flags: --config, --profile (override config/profile path) per CONTEXT; -q suppresses progress; when --json, errors as JSON to stderr.
  </action>
  <verify>neurohid-service config show (with default or --config path); neurohid-service config validate (valid file exits 0, invalid exits 3); neurohid-service pipeline run --dry-run exits 0 when config valid. With --json, invalid config yields JSON error on stderr.</verify>
  <done>neurohid config show and neurohid config validate work (via dispatch from neurohid binary); pipeline run --dry-run or pipeline validate available; exit codes and --json error output per CONTEXT.</done>
</task>

</tasks>

<verification>
- cargo check --workspace; cargo test --workspace; cargo clippy --workspace -- -D warnings.
- neurohid config show; neurohid config validate; neurohid config validate --config /nonexistent (exit 3 or non-zero); neurohid pipeline run --dry-run.
</verification>

<success_criteria>
- COMP-02 satisfied: developer can configure signal pipeline and decoder via SDK/CLI and documented config format.
- must_haves truths and artifacts present; YAML and TOML both supported; pipeline/decoder scope documented.
</success_criteria>

<output>
After completion, create `.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-02-SUMMARY.md`
</output>