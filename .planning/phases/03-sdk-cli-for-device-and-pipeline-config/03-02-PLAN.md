---
phase: 03-sdk-cli-for-device-and-pipeline-config
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - crates/neurohid-storage/src/config.rs
  - crates/neurohid-storage/Cargo.toml
  - docs/formats/config-format.md
  - crates/neurohid-sdk/src/lib.rs
  - crates/neurohid-sdk/src/config.rs
  - crates/neurohid/src/cli.rs
  - crates/neurohid/Cargo.toml
autonomous: true
requirements:
  - COMP-02

must_haves:
  truths:
    - Developer can load and save system config (decoder + signal) via SDK
    - Developer can validate config and view config via CLI (neurohid config validate, config show)
    - Config file format supports both YAML and TOML with same schema and format_version
    - Pipeline/decoder config scope (decoder model path and params, signal preprocessing options) is documented
  artifacts:
    - path: crates/neurohid-storage/src/config.rs
      provides: Config load/save with YAML and TOML
      contains: format detection by extension or param, format_version on save
    - path: docs/formats/config-format.md
      provides: Documented pipeline and decoder scope
      contains: DecoderConfig and SignalConfig fields (model path, params, filters, feature window, etc.)
    - path: crates/neurohid-sdk/src/config.rs
      provides: Public config load/save or get/set API
    - path: crates/neurohid/src/cli.rs
      provides: config set, config validate, config show; pipeline run --dry-run or validate
  key_links:
    - from: ConfigStore
      to: SystemConfig and format_version
      via: same schema for YAML and TOML; always set format_version when saving
    - from: CLI config commands
      to: ConfigStore and config file path
      via: --config/--profile override, load then validate or show
---

<objective>
Enable developers to configure the signal pipeline and decoder via SDK and CLI using a documented config format that supports both YAML and TOML.

Purpose: Satisfy COMP-02 (configure signal pipeline and decoder via SDK/CLI and documented config format). Per CONTEXT: decoder (model path, params) plus signal preprocessing options where the stack exposes them; config file format supports both YAML and TOML. Extend ConfigStore for YAML; document pipeline/decoder scope in config-format.md; expose config API in SDK; add config and pipeline CLI subcommands with validate and dry-run.

Output: ConfigStore loads/saves by format (extension .yaml/.yml => YAML, else TOML; or explicit format param); config-format.md documents decoder and signal sections; neurohid-sdk config module for load/save; neurohid config set, config validate, config show and pipeline subcommands (e.g. pipeline run --dry-run, neurohid config validate) per CONTEXT.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-CONTEXT.md
@.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-RESEARCH.md
@crates/neurohid-types/src/config.rs
@crates/neurohid-storage/src/config.rs
@docs/formats/config-format.md
@docs/crate-boundaries.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config format YAML support and pipeline/decoder documentation</name>
  <files>crates/neurohid-storage/src/config.rs, crates/neurohid-storage/Cargo.toml, docs/formats/config-format.md</files>
  <action>
- Add YAML support to ConfigStore. Add dependency serde_yaml to neurohid-storage (cargo add serde_yaml). In load(): detect format by path extension (.yaml, .yml => YAML; otherwise TOML). In save(): use same extension as path or default TOML; serialize with same SystemConfig schema; always include format_version when saving (same as existing TOML path). Do not change format_version semantics; YAML and TOML share the same schema and version per RESEARCH.
- Extend docs/formats/config-format.md with a "Pipeline and decoder scope" section that documents: DecoderConfig (model_path, online_learning_enabled, learning_rate, gamma, gae_lambda, update_frequency_steps, batch_size, entropy_coef, value_coef, max_grad_norm) and SignalConfig (buffer_size_samples, notch_filter_enabled/hz, bandpass_*_hz, feature_window_ms, feature_step_ms, artifact_rejection_enabled, artifact_threshold_uv). Reference canonical source crates/neurohid-types/src/config.rs. Add a short note that config can be stored as TOML or YAML with the same schema.
- Keep compatibility policy (N=2 previous versions) and existing TOML behavior; new code paths must set format_version when writing.
  </action>
  <verify>cargo check -p neurohid-storage; cargo test -p neurohid-storage; save/load round-trip with .yaml path uses serde_yaml and preserves format_version; config-format.md contains decoder and signal section documentation</verify>
  <done>ConfigStore supports YAML and TOML; format_version present on save; config-format.md documents pipeline/decoder scope and YAML/TOML note.</done>
</task>

<task type="auto">
  <name>Task 2: SDK config API</name>
  <files>crates/neurohid-sdk/src/lib.rs, crates/neurohid-sdk/src/config.rs</files>
  <action>
Expose a public config API from neurohid-sdk that allows loading and saving system config. Options: (a) re-export ConfigStore and DataPaths from storage feature and document usage, or (b) add a thin config module in neurohid-sdk that wraps ConfigStore::load/save/update with a simple load(path?) / save(path?, config) / get_default_path() style API. Use (b) if a simpler facade improves ergonomics; otherwise (a). Ensure the SDK config API uses the same ConfigStore so YAML/TOML and format_version behavior are consistent. Gate behind existing "storage" feature. Document in module doc that config format is documented in docs/formats/config-format.md and supports YAML and TOML.
  </action>
  <verify>cargo check -p neurohid-sdk --features storage; cargo test -p neurohid-sdk --features storage; SDK config API is documented and usable for load/save</verify>
  <done>Developer can load and save SystemConfig via neurohid-sdk (storage feature); docs point to config-format.md.</done>
</task>

<task type="auto">
  <name>Task 3: CLI config and pipeline subcommands</name>
  <files>crates/neurohid/src/cli.rs, crates/neurohid/Cargo.toml</files>
  <action>
Add config and pipeline subcommands to the CLI introduced in 03-01 (neurohid binary when first arg is device/config/pipeline/control). Per CONTEXT:
- neurohid config set: set specific config keys or load from file and save (e.g. --config path, --profile path; key/value or file input as needed for minimal useful set).
- neurohid config validate: load config from file (or default path), validate schema and compatibility; exit 0 if valid, non-zero with documented code (e.g. 3 = config invalid) if invalid. With --json, write JSON result to stdout.
- neurohid config show: load and print current config (human-readable or --json). Progress on stderr, result on stdout; -q suppresses progress.
- neurohid pipeline run --dry-run (or neurohid pipeline validate): validate pipeline/decoder config without starting runtime; same exit code convention. Document in help.
- Config source: config file as main source; --config and --profile override. Use ConfigStore (with YAML/TOML support from Task 1). When --json, machine-readable errors to stderr with code, message, details. Follow same -v/-q and exit code conventions as device subcommands.
  </action>
  <verify>neurohid config --help; neurohid config validate (with valid and invalid config); neurohid config show; neurohid pipeline --help; neurohid pipeline run --dry-run or pipeline validate; cargo build -p neurohid; cargo clippy -p neurohid -- -D warnings</verify>
  <done>neurohid config set, config validate, config show and neurohid pipeline run --dry-run (or pipeline validate) work; config file and flags respected; validate exits with documented codes.</done>
</task>

</tasks>

<verification>
- cargo check --workspace; cargo test --workspace; cargo clippy --workspace -- -D warnings
- Config load/save works for both .toml and .yaml; format_version present in saved files
- neurohid config validate and config show run successfully; pipeline dry-run/validate runs
- docs/formats/config-format.md includes decoder and signal scope
</verification>

<success_criteria>
- COMP-02 satisfied: Developer can configure signal pipeline and decoder (model path, params) via SDK and CLI and documented config format (YAML + TOML)
- Must_haves truths and artifacts present; key links (ConfigStore→format_version, CLI→ConfigStore) implemented
</success_criteria>

<output>
After completion, create `.planning/phases/03-sdk-cli-for-device-and-pipeline-config/03-02-SUMMARY.md`
</output>
