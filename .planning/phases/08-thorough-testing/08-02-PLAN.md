---
phase: 08-thorough-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-core/tests/pipeline_integration.rs
  - crates/neurohid-core/Cargo.toml
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Pipeline boundary (device→signal→decoder→action) is exercised in one integration test"
    - "IPC and config integration tests remain in CI and are stable"
  artifacts:
    - path: crates/neurohid-core/tests/pipeline_integration.rs
      provides: "Pipeline integration test"
      min_lines: 40
  key_links:
    - from: crates/neurohid-core/tests/pipeline_integration.rs
      to: neurohid-core pipeline types
      via: "mock device and in-memory pipeline"
      pattern: "device|signal|decoder|action"
---

<objective>
Add integration tests at the key boundary device→signal→decoder→action and confirm IPC/config boundaries are covered in CI (TEST-02).

Purpose: Catch interface mismatches at the pipeline boundary in CI; research identifies gap of "no pipeline-level integration test."
Output: New integration test in neurohid-core that wires mock device → signal → decoder → action; IPC and config tests confirmed running in CI.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-thorough-testing/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pipeline integration test in neurohid-core</name>
  <files>crates/neurohid-core/tests/pipeline_integration.rs</files>
  <action>
    - Create crates/neurohid-core/tests/pipeline_integration.rs (or equivalent name) that exercises device→signal→decoder→action in one flow.
    - Use mock device and in-memory pipeline per 08-RESEARCH recommendation (neurohid-core test with mock device and in-memory pipeline to avoid full binary).
    - Reuse existing patterns: ephemeral resources, condition-based wait with deadline (no sleep-only), and types from neurohid-signal, neurohid-core/tasks (decoder, signal) as needed.
    - Add any dev-dependencies to crates/neurohid-core/Cargo.toml required for the test (e.g. tokio, tempfile if not already present).
    - Keep test deterministic: no shared global state; use unique temp dir or in-memory store if config is involved.
  </action>
  <verify>`cargo nextest run -p neurohid-core --test pipeline_integration` (or `cargo test -p neurohid-core --test pipeline_integration`) passes; test is included in CI when Test job runs neurohid-core tests.</verify>
  <done>Pipeline integration test exists, runs in CI with workspace tests, and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm IPC and config integration tests in CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    - Verify that existing IPC integration tests (neurohid-ipc transport, IPC compat matrix with Python test_control_client/test_bridge) and config roundtrip tests (neurohid-storage ConfigStore save/load) are run in CI and are stable.
    - If CI does not already run neurohid-storage config tests as part of workspace tests, confirm they run (e.g. cargo test -p neurohid-storage) and document in plan summary. Do not remove or skip existing boundary tests.
    - Only add CI changes if a boundary test is missing from CI; otherwise document "IPC and config boundaries covered by existing jobs" in SUMMARY.
  </action>
  <verify>CI runs neurohid-ipc and neurohid-storage tests (via workspace or explicit -p); IPC compat matrix and config roundtrip are executed.</verify>
  <done>IPC and config integration tests are executed in CI and remain stable.</done>
</task>

</tasks>

<verification>
- Run full workspace tests including neurohid-core pipeline_integration, neurohid-ipc, neurohid-storage.
- Confirm no regression in IPC compat matrix or Python bridge tests.
</verification>

<success_criteria>
- TEST-02 satisfied: integration tests at IPC, pipeline, and config boundaries run in CI; pipeline test is new, others confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-thorough-testing/08-02-SUMMARY.md`
</output>
