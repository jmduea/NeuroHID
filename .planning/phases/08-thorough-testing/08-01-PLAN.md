---
phase: 08-thorough-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - nextest.toml
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Rust tests run via cargo-nextest with retries and timeouts in CI"
    - "Local and CI test runs are deterministic (no sleep-only waits in new tests)"
  artifacts:
    - path: nextest.toml
      provides: "Retries and run timeout for integration tests"
      contains: "retries"
    - path: .github/workflows/ci.yml
      provides: "Test job invoking nextest"
      pattern: "nextest"
  key_links:
    - from: .github/workflows/ci.yml
      to: nextest
      via: "Test and Test (macOS) job steps"
      pattern: "nextest run"
---

<objective>
Adopt cargo-nextest for Rust tests and optional nextest.toml so developers get deterministic, non-flaky runs with retries and timeouts (TEST-01).

Purpose: Satisfy TEST-01 (deterministic test runs via test policy and tooling); establish baseline for Phase 8.
Output: CI Test and Test (macOS) jobs run `cargo nextest run --workspace`; repo-root nextest.toml with retries and run timeout; extension_outlet_e2e still run (via nextest).
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-thorough-testing/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nextest and switch CI Test jobs to nextest</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    - Install cargo-nextest in CI (use taiki-e/install-action or equivalent for nextest; research recommends 0.9.x).
    - In Test and Test (macOS) jobs: replace "cargo test --workspace" with "cargo nextest run --workspace"; keep the separate "Extension outlet e2e" step but run it via nextest (e.g. "cargo nextest run -p neurohid-core --test extension_outlet_e2e") so one runner is used.
    - Do not change rust-coverage job yet (08-RESEARCH: keep coverage job as-is initially; nextest for Test job only).
    - Use uv/rtk per AGENTS.md where applicable; Windows/Linux/macOS matrix must all use nextest.
  </action>
  <verify>Push or PR triggers CI; Test and Test (macOS) jobs run and pass with nextest; `cargo nextest run --workspace` passes locally if nextest is installed.</verify>
  <done>CI Test jobs invoke cargo nextest; workspace and extension_outlet_e2e tests run under nextest.</done>
</task>

<task type="auto">
  <name>Task 2: Add nextest.toml with retries and timeout</name>
  <files>nextest.toml</files>
  <action>
    - Create nextest.toml at repository root (or .config/nextest.toml per project convention; prefer repo root for visibility).
    - Set retries (e.g. backoff = "fixed", count = 2) and run.tests.timeout (e.g. period = "60s", terminate-after = 3) per 08-RESEARCH code example so integration tests have bounded time and limited retries without masking flakiness.
    - Do not add broad retries for all tests; keep policy minimal so flakiness is visible and fixable.
  </action>
  <verify>`cargo nextest run --workspace` respects nextest.toml (timeout/retries apply); no regression in test pass rate.</verify>
  <done>nextest.toml exists with retries and run timeout; tests still pass.</done>
</task>

</tasks>

<verification>
- Run `cargo nextest run --workspace` and `cargo nextest run -p neurohid-core --test extension_outlet_e2e` locally (with nextest installed).
- Confirm CI Test and Test (macOS) use nextest and succeed on a branch.
</verification>

<success_criteria>
- TEST-01 tooling in place: nextest is the Test job runner; nextest.toml provides retries and timeout.
- Deterministic baseline: no new sleep-only waits introduced; existing patterns (condition-based wait, ephemeral ports) remain as in 08-RESEARCH.
</success_criteria>

<output>
After completion, create `.planning/phases/08-thorough-testing/08-01-SUMMARY.md`
</output>
