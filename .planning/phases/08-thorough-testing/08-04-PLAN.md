---
phase: 08-thorough-testing
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .github/workflows/ci.yml
  - python/tests/test_e2e_service_client.py
  - crates/neurohid/src/bin/neurohid-service.rs
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "At least one E2E path runs in CI: service started, Python client connects, snapshot or control exercised"
    - "E2E uses existing Rust service and Python client (no new E2E framework)"
  artifacts:
    - path: python/tests/test_e2e_service_client.py
      provides: "E2E test: service + Python client"
      min_lines: 30
  key_links:
    - from: python/tests/test_e2e_service_client.py
      to: neurohid-service binary
      via: "spawn process, connect, snapshot/control"
      pattern: "snapshot|control|connect"
---

<objective>
Add one valuable E2E path: spawn neurohid-service, connect with Python client, exercise snapshot and optionally control (TEST-04).

Purpose: Satisfy TEST-04 (at least one valuable E2E path); use research recommendation (b): spawn service + Python client snapshot/control.
Output: New Python E2E test that starts neurohid-service (or uses existing IPC compat job), connects with control client, requests snapshot; test runs in CI.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-thorough-testing/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add E2E test: spawn neurohid-service and Python client</name>
  <files>python/tests/test_e2e_service_client.py</files>
  <action>
    - Create python/tests/test_e2e_service_client.py (or equivalent name) that: (1) starts neurohid-service as a subprocess (ephemeral port via env or CLI), (2) waits for service ready using condition-based wait (no sleep-only; poll until port listening or runtime snapshot indicates ready), (3) connects with existing Python control client (e.g. from test_control_client/test_bridge patterns), (4) requests snapshot and optionally sends a control message, (5) asserts expected response shape, (6) tears down the service process.
    - Use uv for running tests per AGENTS.md: `uv run --project python pytest python/tests/test_e2e_service_client.py -v`.
    - Use ephemeral port allocation (e.g. bind to 127.0.0.1:0 or pass port from env) so no conflict with other jobs; isolate test (unique temp dir if service writes state).
    - If neurohid-service does not yet support a "ready" signal or port-in-env, extend minimally (e.g. wait for TCP listen on port) and document in SUMMARY.
  </action>
  <verify>`uv run --project python pytest python/tests/test_e2e_service_client.py -v` passes when neurohid-service binary is on PATH or launched from repo root; test uses condition-based wait with deadline.</verify>
  <done>E2E test exists, starts service and Python client, exercises snapshot (and optionally control), passes locally.</done>
</task>

<task type="auto">
  <name>Task 2: Run E2E test in CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    - Add a step or job that runs the new E2E test when Rust and Python are both in scope (e.g. in IPC compat matrix or a dedicated E2E step after Test job that builds neurohid-service and runs `uv run --project python pytest python/tests/test_e2e_service_client.py`).
    - Ensure neurohid-service binary is built and available (e.g. cargo build -p neurohid --bin neurohid-service); ensure Python env has dependencies (uv run --project python).
    - Minimize CI time: run E2E once per platform or only on Linux if cross-platform E2E is fragile; document in plan SUMMARY.
  </action>
  <verify>CI runs the E2E test and it passes; no Playwright or new E2E framework introduced.</verify>
  <done>E2E path (service + Python client) runs in CI and passes.</done>
</task>

</tasks>

<verification>
- Run E2E test locally; trigger CI and confirm E2E step/job passes.
- Confirm single E2E path meets TEST-04 (Hub discover→connect→stream or runtime profile→decoder→action or service+client; this plan implements service+client).
</verification>

<success_criteria>
- TEST-04 satisfied: one valuable E2E path (neurohid-service + Python client snapshot/control) exercised in tests and CI.
</success_criteria>

<output>
After completion, create `.planning/phases/08-thorough-testing/08-04-SUMMARY.md`
</output>
