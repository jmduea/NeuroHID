---
phase: 04-standard-path-and-recording
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - crates/neurohid-core/src/recording/xdf_writer.rs
  - crates/neurohid-core/src/recording/mod.rs
  - crates/neurohid-core/src/lib.rs
  - crates/neurohid/src/bin/neurohid-service.rs
  - crates/neurohid-core/src/tasks/replay_source.rs
  - crates/neurohid-core/src/tasks/mod.rs
  - crates/neurohid-core/src/service.rs
  - docs/user-guide.md
autonomous: true
requirements:
  - PATH-02
must_haves:
  truths:
    - "User can export a session folder to XDF (primary format)"
    - "User can replay a session (virtual source or offline decoder run)"
    - "Exported files and layout are documented for common tools"
  artifacts:
    - path: crates/neurohid-core/src/recording/xdf_writer.rs
      provides: "XDF 1.0 writer from session folder"
      contains: "FileHeader|StreamHeader|write"
    - path: crates/neurohid-core/src/tasks/replay_source.rs
      provides: "File-backed sample source for replay"
      contains: "session|streams|Sample"
  key_links:
    - from: session folder (from 04-02)
      to: XDF file
      via: "export command / xdf_writer"
      pattern: "xdf|export"
    - from: session folder or XDF
      to: pipeline
      via: "replay source or offline run"
      pattern: "replay|virtual.*source"
---

<objective>
Deliver export to XDF and replay: primary format export (XDF 1.0) from session folder, plus replay as virtual source or offline decoder run, and documentation for external tools.

Purpose: Complete PATH-02 — record/export/replay in standard formats for reproducibility.
Output: XDF writer in Rust (against spec); CLI export subcommand; replay backend (file-backed source) and/or offline decoder run; user-guide or docs note on opening exports in EEGLAB/MNE/pyxdf.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-standard-path-and-recording/04-CONTEXT.md
@.planning/phases/04-standard-path-and-recording/04-RESEARCH.md
@crates/neurohid-core/src/tasks/recording.rs
@crates/neurohid-types/src/recording.rs
@crates/neurohid-types/src/signal.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: XDF writer and export CLI</name>
  <files>crates/neurohid-core/src/recording/xdf_writer.rs, crates/neurohid-core/src/recording/mod.rs, crates/neurohid-core/src/lib.rs, crates/neurohid/src/bin/neurohid-service.rs</files>
  <action>
Implement XDF 1.0 writer and expose via CLI:

1. XDF writer (crates/neurohid-core/src/recording/xdf_writer.rs): Implement against the XDF 1.0 spec (https://github.com/sccn/xdf/wiki/Specifications). Input: path to session folder from Plan 04-02 (manifest, config, streams/, actions.jsonl). Output: single .xdf file. Write FileHeader, then for each stream (raw stream data from streams/ plus actions as a stream if desired): StreamHeader (XML with name, type, channel_count, nominal_srate, channel_format, version, source_id, etc.), then Samples and ClockOffset chunks. Use little-endian and chunk layout per spec. Map neurohid_types::signal::Sample to XDF stream format (channel_format float32). Actions can be written as a separate stream (e.g. string or JSON per sample) or documented as available in session folder for analysis. Prefer one XDF with at least the raw streams; actions stream or separate export is acceptable. Do not use the Rust `xdf` crate for writing (it is read-only); implement writer in-tree.

2. Module: Create crates/neurohid-core/src/recording/mod.rs re-exporting xdf_writer (e.g. pub fn export_session_to_xdf(session_dir: &Path, out_path: &Path) -> Result<()>). Add pub mod recording to neurohid-core/src/lib.rs if not already present.

3. CLI: In neurohid-service, add `record export &lt;session_dir&gt; -o &lt;out.xdf&gt;` (or `export &lt;session_dir&gt; -o &lt;out.xdf&gt;` under record). This calls the XDF writer for the given session folder and writes the .xdf file. No running service required for export (offline tool). Print success and path.

  </action>
  <verify>`cargo check -p neurohid-core` passes. Export CLI: `neurohid record export /path/to/session_123 -o out.xdf` produces out.xdf; file can be read by pyxdf (e.g. `uv run -p python -c "import pyxdf; streams, h = pyxdf.load_xdf('out.xdf'); print(len(streams))"`) or Rust xdf crate read path.</verify>
  <done>Session folder can be exported to XDF; CLI exposes export; XDF is valid per spec and readable by common tools.</done>
</task>

<task type="auto">
  <name>Task 2: Replay and external-tools docs</name>
  <files>crates/neurohid-core/src/tasks/replay_source.rs, crates/neurohid-core/src/tasks/mod.rs, crates/neurohid-core/src/service.rs, docs/user-guide.md</files>
  <action>
Implement replay and document external tool usage:

1. Replay as virtual source: Add a file-backed sample source (crates/neurohid-core/src/tasks/replay_source.rs or similar) that reads a session folder (streams/ + manifest for timing) or an XDF file (use Rust `xdf` crate to read). Implement the same interface the device task expects: produce neurohid_types::signal::Sample (or equivalent) on demand so the pipeline can run decoder on replayed data. Integrate as an optional "replay" or "file" device backend: when configured for replay, service spawns this instead of (or in addition to) live device and feeds its output into the same sample path. Prefer session folder as primary input (consistent with 04-02) with XDF support via xdf crate for read. Document in code and/or user-guide how to run runtime in "replay mode" (e.g. config or CLI flag pointing to session dir or .xdf).

2. Offline decoder run (optional but recommended): Provide a way to run the decoder on a recorded session without live pipeline — e.g. CLI `neurohid record replay-offline &lt;session_dir&gt;` or small script that loads session streams, runs decoder inference, and outputs actions (e.g. to stdout or file) for comparison with recorded actions.jsonl. Can be Rust (neurohid-core or neurohid binary) or documented Python (pyxdf load + call into decoder). Prefer at least one working path (CLI or doc).

3. Docs: In docs/user-guide.md (from 04-01) add a short subsection on recording and export: where session folders live, how to export to XDF, and that exported .xdf files open in common tools (EEGLAB, MNE-Python with mne.io.read_raw_xdf, Python with pyxdf). No separate "reproducibility" subsection — reproducibility is side effect of recording/export per CONTEXT.
  </action>
  <verify>Replay source exists and can feed session (or XDF) data into pipeline; offline run path exists or is documented. User-guide mentions export and opening in EEGLAB/MNE/pyxdf.</verify>
  <done>User can replay session (virtual source or offline run); exported XDF and layout documented for external tools.</done>
</task>

</tasks>

<verification>
- XDF export produces valid .xdf readable by pyxdf or xdf crate.
- Replay source feeds session (or XDF) into pipeline when configured.
- Offline decoder run available or documented.
- User-guide (or equivalent) documents export and external tools.
</verification>

<success_criteria>
- PATH-02 fully satisfied: User can record (04-02), export to XDF and replay or analyze in standard formats for reproducibility.
- XDF is primary format; replay supports validation and reproducible demos.
</success_criteria>

<output>
After completion, create `.planning/phases/04-standard-path-and-recording/04-03-SUMMARY.md`
</output>
