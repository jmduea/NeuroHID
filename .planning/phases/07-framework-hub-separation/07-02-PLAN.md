---
phase: 07-framework-hub-separation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .github/scripts/check-framework-boundary.ps1
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - FRAME-03
must_haves:
  truths:
    - "Hub depends only on allowlisted crates; no direct path deps on component crates outside the list"
    - "Adding a disallowed direct dep to Hub (or binaries) fails CI until fixed"
  artifacts:
    - path: .github/scripts/check-framework-boundary.ps1
      provides: "Script that enforces Hub (and optionally neurohid) path deps ⊆ allowlist"
    - path: .github/workflows/ci.yml
      provides: "CI job that runs the boundary check when Rust code or allowlist changes"
  key_links:
    - from: .github/scripts/check-framework-boundary.ps1
      to: .github/framework-allowlist.toml
      via: "Reads allowlist from TOML (single source of truth from 07-01)"
    - from: .github/workflows/ci.yml
      to: .github/scripts/check-framework-boundary.ps1
      via: "Job step invokes script; fail on non-zero exit"
---
<objective>
Enforce the framework boundary in CI: a script asserts that neurohid-hub (and the neurohid binaries crate) have no path dependencies outside the documented allowlist; CI runs the script and fails on violation.

Purpose: Satisfy FRAME-03 (Hub depends only on core/calibration/facade; dependency audit or CI check enforces no disallowed direct deps). No permanent exceptions: fix by re-export from core or by updating allowlist and code together.
Output: .github/scripts/check-framework-boundary.ps1 and a new CI job in .github/workflows/ci.yml.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-framework-hub-separation/07-CONTEXT.md
@.planning/phases/07-framework-hub-separation/07-RESEARCH.md
@.github/workflows/ci.yml
@.github/framework-allowlist.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dependency allowlist check script</name>
  <files>.github/scripts/check-framework-boundary.ps1</files>
  <action>
Create a PowerShell script that enforces the framework boundary by checking path dependencies of specified packages against the allowlist.

1) Read the allowlist from `.github/framework-allowlist.toml` (created in 07-01). Parse the TOML to get allowed path dependency names for `neurohid-hub` and for the `neurohid` package (binaries). Per RESEARCH: use one canonical source; the script must not hardcode a duplicate list — load from the TOML so it stays in sync with docs.

2) Get direct path dependencies: run `cargo metadata --format-version=1`. From the metadata, for each package name to check (neurohid-hub, neurohid), find that package's direct dependencies that are path/workspace members (e.g. dependency where source is workspace root or path). Cargo metadata gives packages[].dependencies; resolve package id to name and filter to workspace members. Alternatively parse each package's Cargo.toml for [dependencies] path = "..." entries — either approach is acceptable as long as it's accurate for workspace path deps only.

3) For each checked package, assert every path dependency is in that package's allowlist. If any path dep is not in the allowlist, write to stderr which package and which dependency are disallowed, and exit with code 1. Otherwise exit 0.

4) Document in script comments that the allowlist is defined in .github/framework-allowlist.toml and must match docs/framework-surface.md; no permanent exceptions (CONTEXT).
  </action>
  <verify>Run .github/scripts/check-framework-boundary.ps1 from repo root; exit 0 with current Cargo.toml state. Temporarily add a disallowed path dep (e.g. neurohid-hub -> neurohid-device) and run script again; exit non-zero and error message lists disallowed dep.</verify>
  <done>Script exists, reads allowlist from TOML, and fails when Hub or neurohid has a path dep not in the allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI job for framework boundary check</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add a CI job that runs the framework boundary script so violations block merge.

1) Add a new job (e.g. `framework-boundary`) that:
   - needs: determine-impact
   - if: same as other Rust gates — e.g. `needs.determine-impact.outputs.rust == 'true' || github.event_name == 'push'` so any PR that touches Rust (or push) runs the check.
   - runs-on: same as other Linux jobs (e.g. self-hosted linux or ubuntu-22.04).
   - steps: checkout, run the script (e.g. `./.github/scripts/check-framework-boundary.ps1` with pwsh). No Rust toolchain required for this job if the script uses cargo metadata (cargo is available in PATH on typical runners; if not, add a minimal setup step).

2) Ensure the job is in the same workflow file and follows existing style (env, step names). Do not add a "known exception" or allowlist bypass; failures must be fixed by code or allowlist update.
  </action>
  <verify>Trigger CI (or run the script locally) and confirm framework-boundary job runs; with current deps it passes. Optionally verify job appears in workflow and runs when crates/ or .github/framework-allowlist.toml change (determine-impact rust or automation).</verify>
  <done>CI runs the boundary check on every PR that touches Rust (or on push); disallowed Hub/binaries path deps fail the run.</done>
</task>

</tasks>

<verification>
- Script check-framework-boundary.ps1 runs and exits 0 with current workspace; exits 1 when a disallowed path dep is added.
- CI workflow includes a job that runs the script; job is gated on rust impact or push.
- No exception list or bypass in script or workflow.
</verification>

<success_criteria>
- FRAME-03: Hub depends only on core (and calibration) and the framework facade; dependency audit or CI check enforces no disallowed direct deps from Hub to component crates.
</success_criteria>

<output>
After completion, create `.planning/phases/07-framework-hub-separation/07-02-SUMMARY.md`
</output>
