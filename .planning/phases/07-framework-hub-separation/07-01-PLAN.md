---
phase: 07-framework-hub-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/framework-allowlist.toml
  - docs/framework-surface.md
  - docs/index.md
  - README.md
  - docs/crate-boundaries.md
autonomous: true
requirements:
  - FRAME-01
  - FRAME-02
  - FRAME-04
must_haves:
  truths:
    - "Developer can find a single doc that lists the framework surface (which crates/features to depend on)"
    - "Docs state Hub is one application built on the framework; dependency graph defines the boundary"
    - "Contributors and embedders can read what is framework vs application"
  artifacts:
    - path: docs/framework-surface.md
      provides: "Framework surface and Hub boundary; embedder-oriented 'what do I depend on?'"
      contains: "framework surface, Hub allowlist, conceptual map"
    - path: .github/framework-allowlist.toml
      provides: "Canonical allowlist for CI script; single source of truth"
      contains: "allowed path dependencies for neurohid-hub (and optionally neurohid)"
    - path: docs/index.md
      provides: "Doc index linking to framework boundary"
    - path: README.md
      provides: "Product intro with link to framework boundary for embedders"
  key_links:
    - from: docs/index.md
      to: docs/framework-surface.md
      via: "Markdown link in Architecture / boundary section"
    - from: README.md
      to: docs/framework-surface.md
      via: "Link in Architecture or developer section"
    - from: docs/framework-surface.md
      to: .github/framework-allowlist.toml
      via: "Documented as source of truth for CI allowlist"
---

<objective>
Define the framework surface and Hub boundary in one canonical doc and make it discoverable so embedders and contributors know what to depend on and what is framework vs application.

Purpose: Satisfy FRAME-01 (documented surface, use without Hub internals), FRAME-02 (Hub as one app, boundary in docs), FRAME-04 (docs describe boundary).
Output: docs/framework-surface.md, .github/framework-allowlist.toml, updated docs/index.md, README.md, and docs/crate-boundaries.md with link/relationship to framework surface.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-framework-hub-separation/07-CONTEXT.md
@.planning/phases/07-framework-hub-separation/07-RESEARCH.md
@docs/crate-boundaries.md
@docs/architecture-rust-core.md
@crates/neurohid-core/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create framework allowlist and framework-surface doc</name>
  <files>.github/framework-allowlist.toml, docs/framework-surface.md</files>
  <action>
Create a single source of truth for the Hub (and binaries) allowlist and the framework boundary doc.

1) Create `.github/framework-allowlist.toml` with the canonical allowlist. Use a structure that a script can parse (e.g. `[hub]` / `allowed = ["neurohid-types", "neurohid-core", "neurohid-calibration", "neurohid-storage", "neurohid-ipc"]` and optionally `[binaries]` with same or extended list including "neurohid-hub" for the neurohid crate). Per RESEARCH: allowlist is neurohid-types, neurohid-core, neurohid-calibration, neurohid-storage, neurohid-ipc for Hub; neurohid (binaries) may also depend on neurohid-hub. No other workspace path deps allowed.

2) Create `docs/framework-surface.md` with embedder-primary audience ("I'm building another app; what do I depend on?"). Include:
   - Mental model: framework is composable building blocks (like PyTorch/scikit-learn); Hub is one application built on that framework.
   - Framework surface: list of crates (and optional features) that are part of the framework — the set embedders and Hub may depend on. Reference existing layer map from crate-boundaries (types → components → core → hub/sdk/binary).
   - Hub boundary: Hub is one application; it may depend only on the documented allowlist. State that the allowlist is enforced by CI and defined in `.github/framework-allowlist.toml` so doc and script cannot diverge.
   - Guidance: types from component crates come from `neurohid_core::facade` or core's public API; do not add direct Hub dependency on neurohid-device, neurohid-signal, neurohid-platform.
   - Short conceptual map (e.g. types → components → core → applications [Hub, service, validate]).
Do not add permanent exceptions or "known exception" lists; CONTEXT forbids them.
  </action>
  <verify>File .github/framework-allowlist.toml exists and lists the five Hub-allowed crates; docs/framework-surface.md exists, contains "framework surface", "Hub", "allowlist", and references .github/framework-allowlist.toml; grep -l "framework-surface" docs/framework-surface.md</verify>
  <done>Allowlist TOML and framework-surface.md exist; embedder can read the doc to know which crates to depend on and that Hub is one app with a fixed allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Wire framework boundary into index, README, and crate-boundaries</name>
  <files>docs/index.md, README.md, docs/crate-boundaries.md</files>
  <action>
Make the framework boundary doc discoverable and relate it to existing boundary docs.

1) In `docs/index.md`: Under "Architecture and System Docs" (or equivalent), add a prominent link to the framework boundary doc, e.g. "Framework surface and Hub boundary (what to depend on): [framework-surface.md](framework-surface.md)". Keep existing links to crate-boundaries and architecture-rust-core; the new doc complements them (framework surface = embedder view; crate-boundaries = placement rules).

2) In `README.md`: In the Architecture or "For developers" section, add a one-line link to the framework boundary doc so embedders see it (e.g. "Building another app? See [Framework surface and Hub boundary](docs/framework-surface.md)."). Do not duplicate long policy; link only.

3) In `docs/crate-boundaries.md`: Add a short "Framework surface and Hub boundary" subsection (or a "See also" line) that points to `docs/framework-surface.md` and states that the framework surface doc defines what embedders and Hub may depend on; this doc (crate-boundaries) defines placement and layer map. Ensure no conflicting statements (Hub allowlist in framework-surface is the source of truth).
  </action>
  <verify>grep -r "framework-surface" docs/index.md README.md docs/crate-boundaries.md; links resolve to docs/framework-surface.md</verify>
  <done>Index, README, and crate-boundaries all link to the framework boundary doc; contributors and embedders can find it from the main entry points.</done>
</task>

</tasks>

<verification>
- docs/framework-surface.md exists and describes framework surface, Hub as one app, and allowlist.
- .github/framework-allowlist.toml exists and is the single source for the allowlist.
- docs/index.md and README.md and docs/crate-boundaries.md link to docs/framework-surface.md.
- No permanent exceptions or exception lists in doc or config.
</verification>

<success_criteria>
- FRAME-01: Developer can identify the documented framework surface and use it without depending on Hub internals.
- FRAME-02: Hub is documented as one application; dependency graph and docs define the boundary.
- FRAME-04: Docs describe the framework vs Hub boundary for contributors and embedders.
</success_criteria>

<output>
After completion, create `.planning/phases/07-framework-hub-separation/07-01-SUMMARY.md`
</output>
