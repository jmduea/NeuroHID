---
phase: 06-composable-and-extensible
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-types/src/outlet.rs
  - crates/neurohid-types/src/lib.rs
  - crates/neurohid-types/src/signal_contract.rs
  - crates/neurohid-types/src/decoder_contract.rs
  - crates/neurohid-core/src/extension_registry.rs
  - crates/neurohid-core/src/lib.rs
  - docs/extension-contracts.md
autonomous: true
requirements:
  - COMP-06
  - EXT-01
  - EXT-02

must_haves:
  truths:
    - "Outlet contract is published and implementable by extensions"
    - "Signal preprocessing contract is published and implementable by extensions"
    - "Decoder contract is published and implementable by extensions"
    - "Extension manifest (name-only ID) is defined and parseable"
    - "Registry discovers extensions from configured path(s) and enforces unique names"
    - "Registry lists extensions by slot: outlets, devices, signal preprocessing, decoders"
    - "Duplicate extension names cause discovery to fail with clear error"
  artifacts:
    - path: crates/neurohid-types/src/outlet.rs
      provides: Outlet contract trait and extension manifest type
    - path: crates/neurohid-types (signal_contract.rs or equivalent)
      provides: Signal preprocessing contract trait
    - path: crates/neurohid-types (decoder_contract.rs or equivalent)
      provides: Decoder contract trait
    - path: crates/neurohid-core/src/extension_registry.rs
      provides: Discovery, manifest parse, name uniqueness, list extensions (all four slots)
    - path: docs/extension-contracts.md
      provides: Documented contract and discovery path (default + override) for all four slots
  key_links:
    - from: extension_registry
      to: neurohid-types manifest type
      via: serde deserialize
      pattern: "ExtensionManifest|extension_manifest"
---

<objective>
Publish contracts for all four pipeline slots (outlet, device/acquisition, signal preprocessing, decoder) and extension manifest in neurohid-types, and add a discovery/registry in neurohid-core that scans configured paths, parses manifests, enforces name uniqueness (fail on duplicate), and exposes lists per slot for Hub/CLI.

Purpose: Foundation for COMP-06 and EXT-01/EXT-02 — all four slots have a contract and selection in config/Hub per CONTEXT ("Which slots: All four"); extensions are identified by name only and discovered from a documented path; duplicate names are an error.

Output: Outlet, signal preprocessing, and decoder contract traits (device already has provider trait) and ExtensionManifest in neurohid-types; ExtensionRegistry in neurohid-core with list_outlets, list_devices, list_signal_preprocessors, list_decoders; docs for all four contracts and discovery path (default + config/env override).
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-composable-and-extensible/06-CONTEXT.md
@.planning/phases/06-composable-and-extensible/06-RESEARCH.md
@crates/neurohid-types/src/config.rs
@crates/neurohid-core/src/tasks/outlet.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Outlet contract and extension manifest in neurohid-types</name>
  <files>crates/neurohid-types/src/outlet.rs, crates/neurohid-types/src/lib.rs</files>
  <action>
Define the outlet/effector contract and extension manifest in neurohid-types.

1. Create `crates/neurohid-types/src/outlet.rs`:
   - Define a trait `Outlet` (or `OutletRunner`) that captures the same inputs as the current `OutletTask`: config (or a minimal outlet config view), and the four channels (sample, feature, action, marker) as broadcast receivers (or equivalent async handles). The trait must have a method that runs until shutdown (e.g. `async fn run(self, shutdown: broadcast::Receiver) -> Result<()>`). Use types already in neurohid-types (OutletConfig, Sample, FeatureVector, Action, StreamMarker) so the contract is in the shared crate. Mark the trait Send + Sync where required by single-threaded or multi-threaded runtime use.
   - Define `ExtensionManifest` struct with at least a string field `name` (the sole ID; no version in ID per CONTEXT) and a field for slot kind (e.g. `kind: ExtensionKind` with variants Outlet | Device | SignalPreprocessing | Decoder) so the registry can list by slot. Use serde for serialization. Document that duplicate names across discovered extensions are an error.
   - Re-export from `lib.rs` (add `pub mod outlet;` and `pub use outlet::{Outlet, ExtensionManifest, ExtensionKind}` or equivalent).

2. Do not change the existing `OutletTask` implementation in neurohid-core in this plan; the next plan will introduce a factory that produces either the built-in OutletTask or a loaded extension implementing this trait.

3. Align trait signature with how `OutletTask::new(config, sample_rx, feature_rx, action_rx, marker_rx)` and `task.run(shutdown).await` are used in `neurohid-core/src/service.rs` so the factory can swap in a trait object.
  </action>
  <verify>cargo check -p neurohid-types succeeds; doc build for neurohid-types shows the new trait and ExtensionManifest.</verify>
  <done>Outlet contract and ExtensionManifest (with kind) are defined in neurohid-types and exported; trait is implementable by a minimal runner that receives config and four channels and runs until shutdown.</done>
</task>

<task type="auto">
  <name>Task 2: Signal preprocessing and decoder contracts in neurohid-types</name>
  <files>crates/neurohid-types/src/signal_contract.rs, crates/neurohid-types/src/decoder_contract.rs, crates/neurohid-types/src/lib.rs</files>
  <action>
Define signal preprocessing and decoder contracts in neurohid-types so all four slots have a published, implementable contract per CONTEXT ("All four — acquisition, signal preprocessing, decoder, output").

1. Create `crates/neurohid-types/src/signal_contract.rs`:
   - Define a trait (e.g. `SignalPreprocessor` or `SignalPipelineRunner`) that captures the contract between device and decoder: accepts raw samples in, produces feature vectors (and optionally forwards samples/markers). Align with how `SignalTask` in neurohid-core consumes samples and emits features (see crates/neurohid-core/src/tasks/signal.rs). The trait should allow running until shutdown (e.g. async run with config and channel handles). Use neurohid-types types (Sample, FeatureVector, StreamMarker, SignalConfig or minimal view). Mark Send + Sync where required.
   - Re-export from lib.rs.

2. Create `crates/neurohid-types/src/decoder_contract.rs`:
   - Define a trait (e.g. `DecoderRunner`) that captures the contract for the decoder slot: accepts feature vectors, produces actions (and integrates with profile/model loading if needed). Align with how `DecoderTask` in neurohid-core consumes features and emits actions (see crates/neurohid-core/src/tasks/decoder.rs). The trait should allow running until shutdown. Use neurohid-types types (FeatureVector, Action, DecoderConfig or minimal view, ProfileId). Mark Send + Sync where required.
   - Re-export from lib.rs.

3. Do not change existing SignalTask or DecoderTask implementations in neurohid-core in this plan; Plan 02 will add factories that produce built-in or extension implementations.
  </action>
  <verify>cargo check -p neurohid-types succeeds; doc build shows both new traits.</verify>
  <done>Signal preprocessing and decoder contracts are defined in neurohid-types and exported; both traits are implementable by extensions.</done>
</task>

<task type="auto">
  <name>Task 3: Extension registry and discovery in neurohid-core</name>
  <files>crates/neurohid-core/src/extension_registry.rs, crates/neurohid-core/src/lib.rs, docs/extension-contracts.md</files>
  <action>
Implement extension discovery and registry in neurohid-core; document discovery path and all four contracts.

1. Create `crates/neurohid-core/src/extension_registry.rs`:
   - Define an `ExtensionRegistry` (or similar) that:
     - Accepts a list of directory paths (e.g. from config or env; default path chosen by planner, e.g. platform-appropriate user config dir like `~/.neurohid/extensions` or project-relative for dev). Document the default and override in docs.
     - On `scan()` or `refresh()`: walks each path for manifest files (e.g. `manifest.json` or `neurohid.manifest.json`), parses each with serde into `ExtensionManifest`, and collects by `name` and kind.
     - Enforces name uniqueness: if the same `name` appears in more than one manifest (across any path), return an error (e.g. `ExtensionError::DuplicateName { name }`) and do not start; no silent deduplication.
     - Exposes list methods for all four slots: `list_outlets()`, `list_devices()`, `list_signal_preprocessors()`, `list_decoders()` (or `list_extensions(kind)` returning names and optionally paths) for Hub/CLI.
   - Dependencies: use neurohid-types for ExtensionManifest and ExtensionKind; use std::fs and serde for scanning/parsing; no libloading in this plan (loading is Plan 02).
   - Wire the registry into the crate root so it can be constructed from config (path list) and used by device, outlet, signal, and decoder factories in Plan 02.

2. Add `docs/extension-contracts.md` (or extend existing architecture doc):
   - Describe all four contracts: outlet (neurohid-types Outlet), device (existing DeviceProvider trait), signal preprocessing (SignalPreprocessor / signal_contract), decoder (DecoderRunner / decoder_contract).
   - Describe extension manifest (name-only ID, kind for slot; no version in ID).
   - Document discovery: where the runtime looks for extensions (default directory, config/env override), that duplicate names are an error, and that rescan happens at startup and on explicit refresh (Hub/CLI in Plan 04).
  </action>
  <verify>cargo check -p neurohid-core succeeds; unit test or small example that constructs registry, scans a temp dir with one valid manifest, and list returns the name; second manifest with same name in another file causes scan to fail with duplicate error. Test that list_signal_preprocessors and list_decoders are present and return by kind.</verify>
  <done>ExtensionRegistry exists, discovers by path list, enforces unique names with clear error, exposes list_outlets, list_devices, list_signal_preprocessors, list_decoders for Hub/CLI; docs describe all four contracts and discovery path.</done>
</task>

</tasks>

<verification>
- cargo check --workspace passes.
- cargo test -p neurohid-core (and neurohid-types if tests added) passes.
- docs/extension-contracts.md exists and describes all four contracts (outlet, device, signal preprocessing, decoder), manifest with kind, and discovery path.
- ExtensionRegistry exposes list_outlets, list_devices, list_signal_preprocessors, list_decoders.
</verification>

<success_criteria>
- All four slot contracts (outlet, device, signal preprocessing, decoder) and ExtensionManifest with kind live in neurohid-types; ExtensionRegistry in neurohid-core discovers extensions, lists by slot, and fails on duplicate names; discovery path is documented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-composable-and-extensible/06-01-SUMMARY.md`
</output>
