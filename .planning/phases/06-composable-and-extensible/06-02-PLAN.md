---
phase: 06-composable-and-extensible
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - crates/neurohid-types/src/config.rs
  - crates/neurohid-core/src/tasks/device.rs
  - crates/neurohid-core/src/tasks/outlet.rs
  - crates/neurohid-core/src/tasks/signal.rs
  - crates/neurohid-core/src/tasks/decoder.rs
  - crates/neurohid-core/src/service.rs
  - crates/neurohid-core/Cargo.toml
autonomous: true
requirements:
  - COMP-06
  - EXT-01
  - EXT-02

must_haves:
  truths:
    - "Config can select device backend by built-in or extension name"
    - "Config can select signal preprocessing by built-in or extension name"
    - "Config can select decoder by built-in or extension name"
    - "Config can select outlet by built-in or extension name"
    - "Device factory loads extension by name via registry and returns Box<dyn DeviceProvider> or fails clearly"
    - "Signal preprocessing factory produces built-in or loaded extension; pipeline and snapshot integrate"
    - "Decoder factory produces built-in or loaded extension; pipeline and snapshot integrate"
    - "Outlet factory produces built-in OutletTask or loaded outlet extension; pipeline and snapshot integrate"
    - "Load/run failure surfaces as explicit error; no silent fallback"
  artifacts:
    - path: crates/neurohid-types/src/config.rs
      provides: DeviceBackend::Extension(name); signal_preprocessing.extension_name; decoder.extension_name; outlet extension selection
    - path: crates/neurohid-core/src/tasks/device.rs
      provides: create_provider resolves Extension(name) via registry, libloading for dylib
    - path: crates/neurohid-core (signal factory / signal loading)
      provides: Signal preprocessing creation from config (built-in vs extension by name)
    - path: crates/neurohid-core (decoder factory / decoder loading)
      provides: Decoder creation from config (built-in vs extension by name)
    - path: crates/neurohid-core (outlet factory / outlet loading)
      provides: Outlet creation from config (built-in vs extension by name)
  key_links:
    - from: service.rs
      to: outlet factory
      via: create outlet from config (built-in or extension)
      pattern: "OutletTask::new|create_outlet"
    - from: service.rs
      to: signal and decoder factories
      via: create signal/decoder from config (built-in or extension)
      pattern: "create_signal|create_decoder|SignalTask::new|DecoderTask::new"
    - from: control snapshot
      to: outlet/device/signal/decoder by name
      via: snapshot includes extension name and status for all four slots
      pattern: "outlet.*name|device.*name|signal.*name|decoder.*name"
---

<objective>
Add name-based selection for all four slots (device, signal preprocessing, decoder, outlet) in config; extend the device provider factory to resolve Extension(name) via the registry (load dylib with libloading) and return Box<dyn DeviceProvider> or fail with a clear error; introduce signal preprocessing and decoder factories that produce built-in or loaded extensions implementing the contracts from 06-01; introduce an outlet factory that produces either the built-in OutletTask or a loaded outlet extension. Pipeline and control/snapshot integrate for all four slots. Fail clearly on load/run — no silent fallback per CONTEXT.

Purpose: Delivers COMP-06 and EXT-01/EXT-02: user can replace any pipeline component (acquisition, signal preprocessing, decoder, output) per CONTEXT "All four slots"; control, Hub, runtime, and observability still integrate (snapshot shows all slot names and status).

Output: Config schema for extension selection for all four slots; device, signal preprocessing, decoder, and outlet factories in core; libloading used for in-process (dylib) plugins; snapshot reflects custom component name and status for each slot.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-composable-and-extensible/06-CONTEXT.md
@.planning/phases/06-composable-and-extensible/06-RESEARCH.md
@crates/neurohid-core/src/tasks/device.rs
@crates/neurohid-core/src/tasks/outlet.rs
@crates/neurohid-core/src/tasks/signal.rs
@crates/neurohid-core/src/tasks/decoder.rs
@crates/neurohid-core/src/service.rs
@crates/neurohid-types/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device config extension selection and create_provider extension path</name>
  <files>crates/neurohid-types/src/config.rs, crates/neurohid-core/src/tasks/device.rs, crates/neurohid-core/Cargo.toml</files>
  <action>
Add device extension selection to config and extend create_provider to load by name.

1. In neurohid-types/src/config.rs:
   - Extend DeviceBackend with a variant for extension by name (e.g. `Extension(String)`) or add an optional `device.extension_name: Option<String>`; when set, backend is treated as "extension" and the string is the sole ID. Per CONTEXT, name only — no version in ID. Ensure serialization/deserialization and Display work for Hub/CLI.

2. In neurohid-core:
   - Add dependency on `libloading` (e.g. 0.8) for loading device provider dylibs. Document that in-process plugins must be built with the same toolchain (RESEARCH: ABI pitfall).
   - In create_provider (or equivalent): when config selects Extension(name), obtain ExtensionRegistry from context (or pass registry into the function that creates the pipeline). Call registry to load the device provider for that name: resolve path to dylib from manifest, use libloading::Library::new(path), load the required symbol (e.g. a C-compatible factory or Rust factory that returns Box<dyn DeviceProvider>). Wrap in a type that holds the Library guard so the library is not unloaded while in use. On unknown name or load error, return Err with a clear message (e.g. "extension not found: {name}" or "failed to load extension {name}: {error}"); never fall back to another backend.
   - Ensure DeviceTask / pipeline receives the registry (or a way to resolve extension by name) so create_provider can be called with it. If registry is not yet injected in this task, add a placeholder or inject it from the top-level service that already has config; planner may introduce a minimal ExtensionRegistry handle in the core runtime so device task can call it.
  </action>
  <verify>cargo check -p neurohid-core -p neurohid-types passes. With a stub or test dylib (or skipped on non-supported platform), create_provider(Extension("x")) returns Err when "x" is unknown and Ok when "x" resolves and loads (or unit test with mock registry).</verify>
  <done>DeviceConfig supports extension by name; create_provider loads device extension via registry and libloading, or fails with clear error; no silent fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Outlet config extension selection and outlet factory</name>
  <files>crates/neurohid-types/src/config.rs, crates/neurohid-core/src/tasks/outlet.rs, crates/neurohid-core/src/service.rs</files>
  <action>
Add outlet extension selection to config; introduce outlet factory; wire into service and snapshot.

1. In neurohid-types/src/config.rs:
   - Add outlet selection for built-in vs extension: e.g. optional `outlet.extension_name: Option<String>`. When set, core uses the extension outlet instead of the built-in OutletTask. When None, use existing built-in (current behavior).

2. In neurohid-core:
   - Refactor outlet creation: introduce a function (e.g. create_outlet) that takes OutletConfig, the four channel receivers, and registry (or resolver). If config has no extension_name, build and return the existing OutletTask wrapped to satisfy the Outlet trait (if needed) or keep OutletTask as the built-in implementation. If extension_name is set, use registry to load the outlet extension (dylib via libloading, same as device); the loaded plugin must implement the Outlet trait from neurohid-types; return Box<dyn Outlet> or a handle that runs the same way (run(shutdown).await). On load/run failure, return Err and do not start the outlet; pipeline must reflect failed/degraded state with explicit reason (snapshot + logs).
   - In service.rs: replace the direct OutletTask::new(...) spawn with the outlet factory; use the same shutdown and channel wiring. Ensure the spawned task runs the selected outlet (built-in or extension) so control snapshot and observability see it (e.g. snapshot includes outlet type/name — "built-in" vs extension name).
   - Ensure control snapshot (ControlSnapshot or equivalent) includes outlet identifier (name or type) and status so Hub and CLI show "outlet: custom" or the extension name; no special handling required for built-in vs extension beyond the name.
  </action>
  <verify>cargo check -p neurohid-core passes. Snapshot or integration test shows outlet name/type; config with unknown outlet extension name fails at startup with clear error; config with built-in outlet runs as today.</verify>
  <done>OutletConfig supports extension by name; outlet factory produces built-in or loaded extension; service spawns it; snapshot shows outlet by name; load failure is explicit, no silent fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Signal preprocessing and decoder config and factories</name>
  <files>crates/neurohid-types/src/config.rs, crates/neurohid-core/src/tasks/signal.rs, crates/neurohid-core/src/tasks/decoder.rs, crates/neurohid-core/src/service.rs</files>
  <action>
Add signal preprocessing and decoder extension selection to config; introduce factories; wire into service and snapshot.

1. In neurohid-types/src/config.rs:
   - Add optional extension selection for signal preprocessing (e.g. `signal_preprocessing.extension_name: Option<String>` or under SignalConfig). When set, core uses the extension instead of the built-in SignalTask pipeline.
   - Add optional extension selection for decoder (e.g. `decoder.extension_name: Option<String>` or under DecoderConfig). When set, core uses the extension instead of the built-in DecoderTask.

2. In neurohid-core:
   - Introduce create_signal_preprocessor (or equivalent) that takes SignalConfig, channel handles, and registry. If no extension_name, build and return the built-in signal pipeline (current SignalTask behavior). If extension_name is set, use registry to load the signal preprocessing extension (dylib via libloading); loaded plugin must implement the SignalPreprocessor trait from neurohid-types; return Box<dyn SignalPreprocessor> or a handle that runs the same way. On load/run failure, return Err; no silent fallback.
   - Introduce create_decoder (or equivalent) that takes DecoderConfig, profile/store, channel handles, and registry. If no extension_name, build and return the built-in DecoderTask. If extension_name is set, load decoder extension via registry (libloading). On load/run failure, return Err; no silent fallback.
   - In service.rs: wire signal and decoder creation through the factories (same as device and outlet). Ensure the spawned signal and decoder tasks use the selected implementation (built-in or extension). Control snapshot (ControlSnapshot or equivalent) must include signal preprocessing and decoder slot identifier (name or type) and status so Hub and CLI show all four slots.

3. Snapshot: Ensure integrity/snapshot reflects the custom component for signal and decoder slots (e.g. "signal: ok" / "signal: custom-name", "decoder: ok" / "decoder: custom-name").
  </action>
  <verify>cargo check -p neurohid-core passes. Snapshot or integration test shows all four slot names (device, signal, decoder, outlet); config with unknown signal or decoder extension name fails at startup with clear error; config with built-in signal and decoder runs as today.</verify>
  <done>SignalConfig and DecoderConfig support extension by name; signal and decoder factories produce built-in or loaded extension; service spawns them; snapshot shows all four slots by name; load failure is explicit, no silent fallback.</done>
</task>

</tasks>

<verification>
- cargo check --workspace and cargo test --workspace pass.
- Device, signal preprocessing, decoder, and outlet can be selected by extension name in config; load failure produces explicit error; snapshot reflects all four slot names and status.
</verification>

<success_criteria>
- Config selects all four slots (device, signal preprocessing, decoder, outlet) by built-in or extension name; device, signal, decoder, and outlet factories load extensions via registry (libloading); pipeline, control, and snapshot integrate for all four; no silent fallback on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-composable-and-extensible/06-02-SUMMARY.md`
</output>
