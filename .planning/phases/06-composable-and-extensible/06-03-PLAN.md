---
phase: 06-composable-and-extensible
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - crates/neurohid-outlet-example/Cargo.toml
  - crates/neurohid-outlet-example/src/lib.rs
  - .github/workflows/ci.yml
  - Cargo.toml
autonomous: true
requirements:
  - EXT-03

must_haves:
  truths:
    - "Example outlet plugin exists and implements the outlet contract"
    - "Example plugin is built as part of the workspace"
    - "CI runs with config that uses the example outlet and asserts something observable"
  artifacts:
    - path: crates/neurohid-outlet-example/src/lib.rs
      provides: Minimal outlet implementation (e.g. log-only or no-op)
    - path: crates/neurohid-outlet-example (manifest / build)
      provides: Extension manifest with name; cdylib or shared lib for loading
    - path: .github/workflows (or equivalent CI)
      provides: Build + e2e step for example outlet
  key_links:
    - from: CI e2e
      to: runtime with example outlet config
      via: config selects example by name; assert snapshot or actions
      pattern: "extension|outlet.*example"
---

<objective>
Create one example outlet plugin as a workspace member (e.g. neurohid-outlet-example) that implements the outlet contract from neurohid-types (minimal: log-only or no-op), and add CI that builds it and runs an end-to-end test where the runtime uses the example outlet and asserts something observable (e.g. control snapshot shows the outlet, or actions are logged to file).

Purpose: Delivers EXT-03 — one example plugin exists and is tested in CI to demonstrate the extension path; validates EXT-02 outlet contract.

Output: crates/neurohid-outlet-example with manifest and outlet implementation; CI job that builds and runs runtime with example outlet and passes.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-composable-and-extensible/06-CONTEXT.md
@.planning/phases/06-composable-and-extensible/06-RESEARCH.md
@crates/neurohid-types (outlet contract from 06-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create neurohid-outlet-example crate and implement outlet contract</name>
  <files>crates/neurohid-outlet-example/Cargo.toml, crates/neurohid-outlet-example/src/lib.rs, Cargo.toml</files>
  <action>
Add the example outlet plugin as a workspace member and implement the outlet contract.

1. Create crate `crates/neurohid-outlet-example`:
   - Cargo.toml: name `neurohid-outlet-example`, depends on neurohid-types (for Outlet trait and types). Build as cdylib (or dylib) so neurohid-core can load it with libloading. Add to workspace members in repo root Cargo.toml.
   - src/lib.rs: Implement the Outlet trait (from 06-01). Minimal behavior: e.g. log-only (log received actions/samples to tracing) or no-op (receive and drop). Export the factory symbol required by the loader (e.g. the convention agreed in 06-02 for loading outlet plugins — e.g. a function that returns Box<dyn Outlet> or that the registry expects). Include or emit an extension manifest (name-only); use a distinct name e.g. "neurohid-outlet-example" so it does not clash with built-ins.
   - Ensure the crate builds with the same Rust edition and toolchain as the workspace (ABI alignment per RESEARCH).

2. Document in the crate README or in docs/extension-contracts.md how to build and load this example (path to built dylib, manifest placement for discovery).
  </action>
  <verify>cargo build -p neurohid-outlet-example succeeds; cargo check --workspace passes.</verify>
  <done>neurohid-outlet-example exists, implements Outlet, builds as loadable library; manifest declares name; workspace builds.</done>
</task>

<task type="auto">
  <name>Task 2: CI build and e2e for example outlet</name>
  <files>.github/workflows/ci.yml (or existing workflow file)</files>
  <action>
Add CI steps that build the example plugin and run an end-to-end test with the runtime using it.

1. In the existing CI workflow (e.g. .github/workflows/ci.yml or the one that runs cargo test / integration tests):
   - Ensure the workspace build includes neurohid-outlet-example (cargo build --workspace or explicit -p neurohid-outlet-example).
   - Add an e2e step (or extend an existing one) that: (a) builds the example outlet; (b) prepares a config that selects the outlet by extension name (e.g. outlet.extension_name = "neurohid-outlet-example"); (c) runs the runtime (e.g. neurohid-service or the in-process test harness) with that config; (d) asserts something observable — e.g. control snapshot contains the outlet with that name, or a test sends a control request and checks snapshot, or actions are written to a file and the test checks the file. Prefer snapshot-based assertion if the runtime exposes control snapshot in tests.
   - Use the discovery path agreed in 06-01 (e.g. env or config pointing to the built example's directory so the registry finds the manifest and library).

2. Do not rely on manual steps; the e2e must run in CI and pass. If the runtime currently has no in-process test that runs with a custom config, add a minimal integration test in neurohid-core or neurohid that starts the pipeline with the example outlet and asserts snapshot or outlet presence.
  </action>
  <verify>CI workflow runs and the new e2e step passes; cargo test --workspace passes including the new test if added in crates.</verify>
  <done>CI builds the example and runs runtime with example outlet; test asserts observable behavior (snapshot or outlet name); EXT-03 satisfied.</done>
</task>

</tasks>

<verification>
- cargo build --workspace and cargo test --workspace pass.
- CI runs and e2e step that uses the example outlet passes.
</verification>

<success_criteria>
- One example outlet plugin exists, is built in CI, and is exercised by an e2e test that asserts observable integration (snapshot or outlet name); EXT-03 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-composable-and-extensible/06-03-SUMMARY.md`
</output>
