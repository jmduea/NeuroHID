---
phase: 06-composable-and-extensible
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - crates/neurohid-hub/src/screens/extensions.rs
  - crates/neurohid-hub/src/screens/settings.rs
  - crates/neurohid-hub (nav / app shell for Extensions screen)
  - crates/neurohid/src/bin/neurohid-cli.rs
  - docs/extension-contracts.md
autonomous: true
requirements:
  - COMP-06

must_haves:
  truths:
    - "Hub has an Extensions screen that lists discovered extensions and supports rescan"
    - "Devices and outlet config in Hub show dropdown: built-in + extension by name"
    - "Selection in Hub updates config immediately (sync); no separate save for device/outlet"
    - "CLI can list extensions and trigger refresh; discovery path documented"
  artifacts:
    - path: crates/neurohid-hub (Extensions screen + device/outlet dropdowns)
      provides: Extensions screen; device and outlet selection from registry
    - path: crates/neurohid (CLI)
      provides: extensions list and refresh subcommands or flags
    - path: docs/extension-contracts.md
      provides: Discovery path and CLI usage
  key_links:
    - from: Hub device dropdown
      to: config.device
      via: selection writes config immediately
      pattern: "config\.device|DeviceBackend|extension_name"
    - from: Hub outlet dropdown
      to: config.outlet
      via: selection writes config immediately
      pattern: "config\.outlet|extension_name"
---

<objective>
Add Hub Extensions screen (list discovered extensions, rescan); show device and outlet selection as built-in + extension by name in Devices and outlet config panels; ensure selection updates config immediately (sync per CONTEXT). Add CLI subcommands or flags to list extensions and refresh discovery; document discovery path and CLI usage.

Purpose: COMP-06 — Hub and CLI parity for extensions; user can manage and choose extensions from Hub and headless; no special "no extensions" UI (same UI, shorter list when none).

Output: Extensions screen in Hub; device/outlet dropdowns with extension names; config sync on choice; CLI list/refresh; docs updated.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-composable-and-extensible/06-CONTEXT.md
@crates/neurohid-hub/src/screens/settings.rs
@crates/neurohid/src/bin/neurohid-cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub Extensions screen and device/outlet dropdowns</name>
  <files>crates/neurohid-hub/src/screens/extensions.rs, crates/neurohid-hub/src/screens/settings.rs, crates/neurohid-hub (app shell / nav)</files>
  <action>
Add Extensions screen and wire device/outlet selection to config with immediate sync.

1. Extensions screen:
   - Create a new screen (e.g. crates/neurohid-hub/src/screens/extensions.rs) that lists discovered extensions from the registry (outlet and device extensions). Use the same registry/list API that Plan 01/02 expose (e.g. from service or IPC). Show extension name and optionally kind (outlet/device). Provide a "Rescan" or "Refresh" button that triggers discovery refresh (on demand per CONTEXT). Add the Extensions screen to the Hub nav/sidebar (per CONTEXT: "Extensions screen to manage/install").
   - When no extensions are installed, show the same UI with a shorter list (no special "no extensions" state per CONTEXT).

2. Devices panel (in Settings or Devices screen):
   - In the device backend/provider selection UI, extend the dropdown to include discovered device extensions by name (in addition to built-in backends: LSL, Mock, Serial, BrainFlow, Auto). When user selects an extension by name, update config immediately (write config.device to use Extension(name) or equivalent). No separate "Save" for this choice — sync per CONTEXT.

3. Outlet config panel:
   - Similarly, extend the outlet selection (or add one if currently fixed) to show built-in plus discovered outlet extensions by name. On selection, set config.outlet.extension_name (or equivalent) and write config immediately.

4. Ensure the Hub has access to the extension registry/list (e.g. via existing IPC to the service that holds the registry, or by the Hub calling the same discovery paths if it runs in the same process). If the service owns the registry, add an IPC method or snapshot field that returns list of extension names; Hub calls it for the Extensions screen and for populating dropdowns.
  </action>
  <verify>Hub builds; Extensions screen appears in nav; device and outlet dropdowns show built-ins and (when present) extension names; selecting an extension updates config and persists (e.g. config file or in-memory config used by runtime). Manual test: add example extension from 06-03, rescan, select in dropdown, confirm config reflects choice.</verify>
  <done>Extensions screen lists extensions and supports rescan; device and outlet dropdowns include extension names; selection syncs to config immediately.</done>
</task>

<task type="auto">
  <name>Task 2: CLI extensions list/refresh and docs</name>
  <files>crates/neurohid/src/bin/neurohid-cli.rs, docs/extension-contracts.md</files>
  <action>
Add CLI commands to list extensions and refresh discovery; document discovery path and usage.

1. CLI (neurohid or neurohid-cli binary):
   - Add a subcommand or group (e.g. `neurohid extensions list`) that prints discovered extension names (and optionally kind/path). Use the same discovery/registry as core (e.g. load config for extension paths, run scan, print results). Exit 0 with list; if discovery fails (e.g. duplicate names), exit non-zero with clear error.
   - Add a way to trigger refresh (e.g. `neurohid extensions refresh` or `extensions list --refresh`). This may simply re-run discovery and print; for headless, "refresh" can mean "run scan and report current list" since the registry is in-process. Document that Hub/CLI refresh makes new extensions visible without full process restart where applicable.

2. docs/extension-contracts.md (or user-guide):
   - Document where the runtime looks for extensions (default path, config or env override) as agreed in 06-01. Document CLI: `neurohid extensions list`, `neurohid extensions refresh` (or equivalent). Add a short "Adding an extension" section that points to the example plugin and contract.
  </action>
  <verify>Running neurohid extensions list (or equivalent) prints extension names when discovery path contains manifests; running with --refresh or refresh subcommand succeeds; docs describe discovery path and CLI.</verify>
  <done>CLI lists and refreshes extensions; discovery path and CLI usage documented.</done>
</task>

</tasks>

<verification>
- cargo check -p neurohid-hub -p neurohid passes.
- Hub shows Extensions screen and device/outlet dropdowns with extension names; config syncs on selection.
- CLI extensions list/refresh work; docs updated.
</verification>

<success_criteria>
- Hub has Extensions screen and device/outlet selection with immediate config sync; CLI supports list and refresh; discovery path and usage are documented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-composable-and-extensible/06-04-SUMMARY.md`
</output>
