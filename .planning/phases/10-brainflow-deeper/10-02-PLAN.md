---
phase: 10-brainflow-deeper
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/neurohid-device/Cargo.toml
  - crates/neurohid-device/src/brainflow.rs
  - crates/neurohid-device/src/lib.rs
  - docs/brainflow.md
autonomous: true
requirements:
  - BRAIN-06
  - BRAIN-07
user_setup: []

must_haves:
  truths:
    - "Developer can build neurohid-device with brainflow-native and use the real BrainFlow SDK for real hardware"
    - "Default and CI use synthetic only (brainflow-native not enabled); no mock backend"
    - "BrainFlow (synthetic or native) streams into the same signal pipeline as LSL (Device → SampleStream → sample_tx)"
  artifacts:
    - path: crates/neurohid-device/Cargo.toml
      provides: "brainflow-native feature and optional BrainFlow Rust dependency; default does not include brainflow-native"
      contains: "brainflow-native"
    - path: crates/neurohid-device/src/brainflow.rs
      provides: "Native Device/Provider when brainflow-native enabled; synthetic path unchanged when only brainflow"
  key_links:
    - from: crates/neurohid-device/src/brainflow.rs
      to: "Device trait start_streaming() returns SampleStream yielding Sample"
      via: "Native implementation maps get_board_data to Sample; same pipeline as streaming.rs"
      pattern: "start_streaming|SampleStream|Sample"
    - from: crates/neurohid-core/Cargo.toml
      to: "default features"
      via: "Do not add brainflow-native to neurohid-core default or CI"
      pattern: "default.*brainflow"
---

<objective>
Add the real BrainFlow SDK behind a feature flag (brainflow-native) in neurohid-device and implement the native Device/Provider so that the BrainFlow streaming path feeds the same pipeline as LSL (BRAIN-06, BRAIN-07). Default and CI keep using synthetic only; no mock backend.
Purpose: Developers can use real hardware when they build with brainflow-native; same Device → SampleStream → pipeline path; default/CI unchanged.
Output: brainflow-native feature, optional BrainFlow Rust dep, native Device/Provider implementation; docs note default vs native.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brainflow-deeper/10-RESEARCH.md
@.planning/phases/10-brainflow-deeper/10-01-SUMMARY.md
crates/neurohid-device/src/brainflow.rs
crates/neurohid-core/src/tasks/device/streaming.rs
crates/neurohid-types/src/signal.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brainflow-native feature and optional BrainFlow dependency</name>
  <files>crates/neurohid-device/Cargo.toml</files>
  <action>
    Add feature brainflow-native to neurohid-device. Implementation options (choose one and document in code or docs): (a) Git dependency on brainflow-dev/brainflow rust_package at the version pinned in 10-01 (docs/brainflow.md), e.g. brainflow = { git = "https://github.com/brainflow-dev/brainflow", tag = "v5.13.0", optional = true }, or (b) path dependency to a vendored copy of rust_package/brainflow (e.g. third_party/brainflow) with optional = true. Set brainflow-native = ["brainflow"] so that enabling brainflow-native enables the brainflow crate. Do NOT add brainflow-native to default features; default must remain ["lsl"] (or current default). Do NOT add brainflow-native to neurohid-core default or to neurohid-core's brainflow feature propagation — neurohid-core's brainflow feature should continue to mean only neurohid-device/brainflow (synthetic). Ensure neurohid-sdk and CI do not enable brainflow-native unless explicitly for a native-build job. Follow 10-RESEARCH: "Default and CI use synthetic board only."
  </action>
  <verify>cargo build -p neurohid-device succeeds (default); cargo build -p neurohid succeeds; cargo build -p neurohid-device --features brainflow,brainflow-native fails with a clear missing-lib or link error if BrainFlow C++ libs are not present (expected when not built), or succeeds if libs are in place; grep "brainflow-native" crates/neurohid-device/Cargo.toml; default features do not include brainflow-native.</verify>
  <done>brainflow-native feature exists and gates optional BrainFlow crate; default and neurohid-core default do not enable brainflow-native.</done>
</task>

<task type="auto">
  <name>Task 2: Implement native BrainFlow Device and SampleStream</name>
  <files>crates/neurohid-device/src/brainflow.rs, crates/neurohid-device/src/lib.rs</files>
  <action>
    When brainflow-native is enabled, provide a native path that implements Device and yields Sample into the existing pipeline. Per 10-RESEARCH: (1) Use BoardShim (or equivalent from the BrainFlow Rust binding): prepare_session, start_stream, get_board_data (or get_current_board_data) loop, stop_stream, release_session. (2) Map 2D data to neurohid_types::signal::Sample: use get_timestamp_channel(board_id), get_eeg_channels(board_id); one Sample per column (or batched) with device_timestamp, values (EEG row slice), sequence_number from package number channel if available; system_timestamp via neurohid_types::now_micros() if needed. BrainFlow EEG is µV; Sample.values are µV — no conversion. (3) start_streaming() returns a SampleStream (same type as existing pipeline) that yields Result<Sample>; DeviceTask in neurohid-core already consumes SampleStream and sends to sample_tx — no changes there. (4) Implement stop_streaming and disconnect to call stop_stream and release_session. (5) Keep existing synthetic path: when only brainflow is enabled (no brainflow-native), current BrainFlowProvider/BrainFlowDevice using MockDevice unchanged. When brainflow-native is enabled, either: (A) same file with #[cfg(feature = "brainflow-native")] blocks that switch connect() to return a native Device for real boards (e.g. board_id != 0 or serial_port set) and synthetic for board_id 0, or (B) a separate module (e.g. brainflow_native.rs) compiled only under brainflow-native and used by brainflow.rs for the native path. Do not re-export BrainFlow-specific types (e.g. BoardShim) in neurohid-device's public API; wrap everything in DeviceProvider/Device and Sample. Follow crates/AGENTS.md: unsafe blocks need SAFETY comments; prefer Result/? over unwrap in library paths.
  </action>
  <verify>With brainflow-native enabled and BrainFlow libs built: cargo build -p neurohid-device --features brainflow,brainflow-native succeeds; with only brainflow: cargo build -p neurohid-device --features brainflow succeeds and existing tests pass. Code contains mapping from get_board_data (or equivalent) to Sample and a SampleStream implementation; no second pipeline (same Device/start_streaming contract).</verify>
  <done>Native BrainFlow Device exists behind brainflow-native; start_streaming() returns SampleStream yielding Sample; same pipeline as LSL and synthetic; synthetic path unchanged when brainflow-native is off.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm default/CI and document native as optional</name>
  <files>docs/brainflow.md, .github/workflows (if needed)</files>
  <action>
    (1) Confirm neurohid-device and neurohid-core default features do not include brainflow-native; confirm no CI job enables brainflow-native unless it is an optional job that builds BrainFlow then builds with brainflow-native (optional job is out of scope for this task — just ensure main/test jobs do not enable it). (2) In docs/brainflow.md, state clearly that the default build and CI use the synthetic board only; to use real hardware, build with --features brainflow-native after building BrainFlow per the Phase 10 build order (link to the section added in 10-01). Add one sentence that the BrainFlow streaming path (synthetic or native) is the same pipeline as LSL: Device → SampleStream → pipeline.
  </action>
  <verify>grep -r "brainflow-native" .github/workflows/ does not add it to default test/build jobs; docs/brainflow.md states default and CI use synthetic only and native is optional; doc states same pipeline for BrainFlow and LSL.</verify>
  <done>Default and CI remain synthetic-only; docs state native is optional and same pipeline applies.</done>
</task>

</tasks>

<verification>
- cargo build -p neurohid (default) succeeds
- cargo build -p neurohid-device --features brainflow succeeds; existing brainflow tests pass
- brainflow-native is not in default features of neurohid-device or neurohid-core
- Native implementation (when built) implements Device and SampleStream; no second pipeline
- docs/brainflow.md documents default vs native and same pipeline
</verification>

<success_criteria>
- Developer can build and use the real BrainFlow SDK behind brainflow-native; default and CI use synthetic only (BRAIN-06).
- BrainFlow streaming path (synthetic or native) uses the same pipeline as LSL (BRAIN-07).
</success_criteria>

<output>
After completion, create `.planning/phases/10-brainflow-deeper/10-02-SUMMARY.md`
</output>
