---
phase: 10-brainflow-deeper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/brainflow.md
  - scripts/build-brainflow-native.sh
autonomous: true
requirements:
  - BRAIN-08
user_setup: []

must_haves:
  truths:
    - "Developer can reproduce a BrainFlow native build using a pinned version and documented steps"
    - "Build order (C++ then Rust, then neurohid-device) is unambiguous and reproducible"
  artifacts:
    - path: docs/brainflow.md
      provides: "Pinned BrainFlow version and full build order (C++ → Rust → neurohid-device)"
      contains: "brainflow-native"
    - path: scripts/build-brainflow-native.sh
      provides: "Optional script to build C++ and copy libs for reproducible native build"
  key_links:
    - from: docs/brainflow.md
      to: "exact version (git tag or commit)"
      via: "Pinned version subsection or inline"
      pattern: "5\\.13|tag|commit|version"
---

<objective>
Pin the BrainFlow C++/Rust version and document build steps (C++ core then Rust binding, then neurohid-device with brainflow-native) so that builds are reproducible (BRAIN-08). Provide a single canonical reference in docs and an optional script for building the native stack.
Purpose: Reproducible native BrainFlow builds; foundation for 10-02 (real SDK behind feature flag).
Output: docs/brainflow.md updated with pinned version and build order; optional scripts/build-brainflow-native.sh.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brainflow-deeper/10-RESEARCH.md
docs/brainflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document pinned version and build order in docs/brainflow.md</name>
  <files>docs/brainflow.md</files>
  <action>
    Update docs/brainflow.md to add a "Native SDK (Phase 10)" section that: (1) Pins the BrainFlow version — state the exact git tag or commit (e.g. 5.13.0 or the tag that includes both C++ and rust_package) and that neurohid uses this for reproducible builds. (2) Documents build order: (a) C++ — from BrainFlow repo root run `python tools/build.py` (document any required args, e.g. `--brainflow-version`, and that output goes to `installed/lib/` and `installed/inc/`). (b) Rust — the official Rust binding (rust_package/brainflow) expects a `lib/` directory inside the crate; document that after building C++, copy `installed/lib/*` into `rust_package/brainflow/lib/` (or document env-based approach if we later patch build.rs). (c) NeuroHID — build with `cargo build -p neurohid-device --features brainflow,brainflow-native` (and that default/CI do not use brainflow-native). (3) Keep existing "Synthetic vs native" and "Build order" text but make Phase 10 native path the authoritative subsection. Do not remove Phase 9 synthetic content; add the native path as the optional path for real hardware. Reference 10-RESEARCH.md Standard Stack and build order.
  </action>
  <verify>docs/brainflow.md contains a "Native SDK" or "Phase 10" section with pinned version, C++ build step (tools/build.py), Rust step (copy libs or env), and neurohid-device build command; grep -E "5\\.13|tag|commit|build\\.py|brainflow-native" docs/brainflow.md returns matches.</verify>
  <done>Single canonical doc has pinned BrainFlow version and unambiguous C++ → Rust → neurohid-device build order; developer can reproduce native build from the doc.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional reproducible-build script</name>
  <files>scripts/build-brainflow-native.sh</files>
  <action>
    Create scripts/build-brainflow-native.sh (or scripts/build-brainflow-native.ps1 for Windows if preferred; research mentions build.py which is Python, so script can assume Bash on Unix and document Windows in docs). The script must: (1) Accept an optional BRAINFLOW_REPO_DIR (default: current dir or a documented location) and optional BRAINFLOW_VERSION/tag. (2) Run BrainFlow C++ build: `python tools/build.py` with args documented in docs/brainflow.md. (3) Copy `installed/lib/*` into a target directory — either rust_package/brainflow/lib/ inside a vendored BrainFlow repo, or an env-set table location (e.g. BRAINFLOW_LIB_DIR) that neurohid-device's build can use. Document in docs/brainflow.md that this script is optional and that manual steps in the doc suffice. Use the same pinned version as in docs/brainflow.md. Script should be idempotent and non-destructive (e.g. do not overwrite repo files if target is inside a git tree without user consent). Keep script short; if the repo does not vendor BrainFlow, script can output instructions or copy to a well-known path. Follow AGENTS.md: no bare python; use uv for Python if the script invokes Python (e.g. uv run python tools/build.py).
  </action>
  <verify>scripts/build-brainflow-native.sh exists; it runs tools/build.py (or documents that user must run from BrainFlow repo) and copies libs to a documented location; docs/brainflow.md mentions the script as optional.</verify>
  <done>Optional script exists for reproducible C++ build and lib copy; doc references it; builds remain reproducible from doc alone.</done>
</task>

</tasks>

<verification>
- docs/brainflow.md has pinned version and C++ → Rust → neurohid-device build order
- scripts/build-brainflow-native.sh exists and is referenced in docs
- cargo build -p neurohid (default) still succeeds (no change to default features)
</verification>

<success_criteria>
- BrainFlow version and build steps are pinned and documented; a developer can reproduce a native build from docs (and optionally the script).
- BRAIN-08 satisfied: reproducible builds.
</success_criteria>

<output>
After completion, create `.planning/phases/10-brainflow-deeper/10-01-SUMMARY.md`
</output>
