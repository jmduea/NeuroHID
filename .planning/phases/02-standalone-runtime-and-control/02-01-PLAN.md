---
phase: 02-standalone-runtime-and-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid/src/bin/neurohid-service.rs
  - docs/deployment-guide.md
autonomous: true
requirements:
  - RUNT-01
user_setup: []

must_haves:
  truths:
    - "User can start the standalone runtime with a chosen profile and have it run without the Hub GUI"
    - "Control endpoint is reachable when running standalone (so status and output toggle work without Hub)"
  artifacts:
    - path: crates/neurohid/src/bin/neurohid-service.rs
      provides: "Service entrypoint; control server binding"
      contains: "resolve_runtime_ipc_server_config"
    - path: docs/deployment-guide.md
      provides: "Startup and control endpoint documentation"
  key_links:
    - from: neurohid-service main flow
      to: "control server"
      via: "run_managed_runtime → resolve_runtime_ipc_server_config"
      pattern: "resolve_runtime_ipc_server_config"
    - from: docs/deployment-guide.md
      to: "profile and control"
      via: "documented startup and --control-port / default"
---

<objective>
Ensure the standalone runtime (neurohid-service) starts with a reachable control endpoint by default when config does not set one, and document how to start with profile (decoder from profile) and control so RUNT-01 is satisfied without requiring the Hub.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-standalone-runtime-and-control/02-RESEARCH.md
@crates/neurohid/src/bin/neurohid-service.rs
@docs/deployment-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default control endpoint when running standalone</name>
  <files>crates/neurohid/src/bin/neurohid-service.rs</files>
  <action>
In run_managed_runtime, when control_port is None and service_config.ipc_endpoint is empty (after trim), use a default TCP port so the control server is started. Use 47384 as the default (already referenced in deployment-guide and research).

- Before calling resolve_runtime_ipc_server_config, compute effective_control_port: if args.control_port is Some, use it; else if service_config.ipc_endpoint.trim().is_empty(), use Some(47384); else None. Pass effective_control_port into resolve_runtime_ipc_server_config.
- Add a named constant (e.g. DEFAULT_STANDALONE_CONTROL_PORT = 47384) near the top of the file and use it.
- Do not change resolve_runtime_ipc_server_config signature; only change the caller so that when no config endpoint and no --control-port are set, the service still binds control on 127.0.0.1:47384.
  </action>
  <verify>cargo check -p neurohid --bin neurohid-service succeeds. Start service with no config and no --control-port (e.g. cargo run -p neurohid --bin neurohid-service -- --profile some-profile 2>&1 or with minimal config that omits service.ipc_endpoint); confirm in logs that "Starting unified IPC v3 server" appears with endpoint 127.0.0.1:47384.</verify>
  <done>Standalone run with empty config endpoint binds control server on default port 47384 so control is reachable without Hub.</done>
</task>

<task type="auto">
  <name>Task 2: Document standalone startup and control</name>
  <files>docs/deployment-guide.md</files>
  <action>
In deployment-guide.md, add or expand a "Standalone runtime (without Hub)" section that:

- States that the user can start the standalone runtime with a chosen profile and that the decoder is loaded from that profile (no separate "attach decoder" step; profile implies decoder). Use wording that matches RUNT-01.
- Documents how to start: --config (optional), --profile (optional), --control-port (optional). Note that when config does not set service.ipc_endpoint and --control-port is not passed, the service defaults to TCP 127.0.0.1:47384 for control so status and output toggle are available without the Hub.
- Keeps or references the existing "Local Control Endpoint" / control request envelope content; ensure the doc states that control is available for snapshot (status) and set_output_enabled (output toggle) without opening the Hub.

Follow docs/AGENTS.md: one canonical home; link to protocol-and-api if needed. Do not duplicate long JSON blocks if they already exist; reference them.
  </action>
  <verify>Read deployment-guide.md and confirm: (1) profile = decoder source is stated, (2) startup options and default control port are documented, (3) control without Hub (status, output toggle) is stated.</verify>
  <done>Deployment guide explains how to run standalone with profile and how control is reachable (default or --control-port) for status and output toggle without Hub.</done>
</task>

</tasks>

<verification>
- With no config and no --control-port, neurohid-service logs show IPC server starting on 127.0.0.1:47384.
- deployment-guide.md contains a standalone-runtime section that covers profile, decoder-from-profile, and control endpoint (default 47384 or --control-port).
</verification>

<success_criteria>
- RUNT-01: User can start standalone runtime with chosen profile (decoder from profile) and have it run without Hub; control is reachable by default so RUNT-02/RUNT-03 can be used without Hub.
</success_criteria>

<output>
After completion, create .planning/phases/02-standalone-runtime-and-control/02-standalone-runtime-and-control-01-SUMMARY.md
</output>
