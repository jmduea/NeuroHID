---
phase: 01-contracts-and-versioned-formats
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/formats/stream-semantics.md
  - crates/neurohid-device/src/lsl/device.rs
autonomous: true
requirements:
  - COMP-04
user_setup: []

must_haves:
  truths:
    - "Stream consumption, timestamp, and 'latest sample' semantics are documented and consistent"
    - "LSL consumption model, timestamps, and drain-then-last (or defined) latest-sample behavior are documented"
  artifacts:
    - path: docs/formats/stream-semantics.md
      provides: "Stream semantics: consumption model, timestamps, latest-sample, overflow/drops"
      min_lines: 80
    - path: crates/neurohid-device/src/lsl/device.rs
      provides: "LSL device implementation; optional doc reference"
  key_links:
    - from: docs/formats/stream-semantics.md
      to: "COMP-04"
      via: "doc defines consumption, timestamps, latest-sample per stream type"
      pattern: "consumption|timestamp|latest sample|drain"
    - from: crates/neurohid-device/src/lsl/device.rs
      to: docs/formats/stream-semantics.md
      via: "comment or doc link so implementation aligns with spec"
      pattern: "stream-semantics|pull_sample|0\\.2"
---

<objective>
Document stream consumption model, timestamps, and "latest sample" semantics for LSL (and briefly for other stream types) so SDK/CLI and runtime behavior are predictable and consistent.

Purpose: Satisfy COMP-04; document drain-then-last (or current runtime behavior) for LSL and any overflow/drop behavior.
Output: docs/formats/stream-semantics.md (structured: schema/BNF + prose); optional code reference in LSL device.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contracts-and-versioned-formats/01-CONTEXT.md
@.planning/phases/01-contracts-and-versioned-formats/01-RESEARCH.md
@crates/neurohid-device/src/lsl/device.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write stream semantics document (LSL full, others brief)</name>
  <files>docs/formats/stream-semantics.md</files>
  <action>
Create docs/formats/ if needed. Write stream-semantics.md with structured style (BNF/schema plus brief prose) per user decision. For LSL (required): (1) Consumption model — blocking pull_sample(timeout) in a loop; samples in order; pull_sample(0.0) returns 0.0 when no sample (non-blocking). (2) Timestamps — LSL returns remote capture time in seconds (LSL clock); time_correction maps to local; NeuroHID converts to microseconds in Sample.device_timestamp and sets system_timestamp. (3) Latest sample — define explicitly: e.g. "drain-then-last" = pull_sample(0.0) until 0.0 then use last sample; state what the current NeuroHID LSL device does (continuous pull with 0.2s timeout, every sample forwarded — document this so consumers know which behavior to expect). (4) Overflow/drops — max_buflen in seconds; buffer fills if consumer slow; document NeuroHID's buffer choice and liblsl drop/block behavior. Use official LSL inlet docs (pull_sample, timestamps, max_buflen). Add short subsections for Serial, BrainFlow, Mock: consumption model, timestamps, latest-sample where applicable, or "same as LSL where applicable" to keep one doc complete.
  </action>
  <verify>File exists; grep for "consumption", "timestamp", "latest sample", "drain", "LSL", "pull_sample" in docs/formats/stream-semantics.md; doc states how NeuroHID obtains samples (continuous pull vs drain-then-last) and meaning of timestamps.</verify>
  <done>Stream semantics are documented for LSL in full and other stream types briefly; consumption, timestamps, and latest-sample are defined.</done>
</task>

<task type="auto">
  <name>Task 2: Align LSL device code with doc and add reference</name>
  <files>crates/neurohid-device/src/lsl/device.rs</files>
  <action>
Add a module-level or key-function comment in the LSL device that references docs/formats/stream-semantics.md for consumption model, timestamps, and latest-sample semantics. Ensure the documented behavior matches the code: current code uses pull_sample(0.2) in a loop and forwards every sample (continuous pull); document that in stream-semantics.md if not already, and do not change runtime behavior in this phase — only document and link. If the code uses a fixed timeout (0.2), note in the doc that "latest sample" for this implementation is the most recently received sample in the continuous stream (not drain-then-last); drain-then-last can be described as the alternative for consumers who want only the latest.
  </action>
  <verify>cargo check -p neurohid-device; LSL device file contains a reference to stream-semantics.md; no change to pull_sample timeout or loop logic unless doc explicitly requires it.</verify>
  <done>LSL device references stream-semantics.md; documented behavior matches implementation.</done>
</task>

</tasks>

<verification>
- docs/formats/stream-semantics.md exists and describes consumption, timestamps, latest-sample, and overflow for LSL (and brief for others).
- LSL device code references the doc; behavior is consistent with documentation.
</verification>

<success_criteria>
- COMP-04 satisfied: stream consumption, timestamp, and "latest sample" semantics are documented and consistent (e.g. drain-then-last for LSL described; current runtime behavior explicitly stated).
- Plan 03 SUMMARY created at .planning/phases/01-contracts-and-versioned-formats/01-03-SUMMARY.md after execution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-versioned-formats/01-03-SUMMARY.md`
</output>
