---
phase: 01-contracts-and-versioned-formats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-types/src/profile.rs
  - crates/neurohid-storage/src/profile.rs
  - docs/formats/profile-format.md
autonomous: true
requirements:
  - COMP-05
  - PATH-03
user_setup: []

must_haves:
  truths:
    - "Profile format has a documented version and compatibility policy"
    - "Calibration and profile metadata are stored with version/identity so the same setup can be reproduced"
    - "Profile metadata and (where applicable) calibration identity roundtrip on export/import"
  artifacts:
    - path: docs/formats/profile-format.md
      provides: "Profile format version, compatibility policy, and reproducibility identity in one doc"
      min_lines: 50
    - path: crates/neurohid-types/src/profile.rs
      provides: "ProfileMetadata with format_version and reproducibility identity fields"
      contains: "format_version"
    - path: crates/neurohid-storage/src/profile.rs
      provides: "Profile load/save and calibration identity handling"
  key_links:
    - from: crates/neurohid-storage/src/profile.rs
      to: crates/neurohid-types/src/profile.rs
      via: "serialize/deserialize ProfileMetadata with format_version"
      pattern: "ProfileMetadata|format_version"
    - from: docs/formats/profile-format.md
      to: "PATH-03"
      via: "doc states where identity is stored and re-run vs audit/share"
      pattern: "reproducibility|identity|re-run|audit"
---

<objective>
Add a format version to profile metadata and document it with a compatibility policy; store and document reproducibility identity (version/identity for profile and calibration) so the same setup can be reproduced (re-run and audit/share).

Purpose: Satisfy COMP-05 for profile and PATH-03 (calibration and profile metadata stored with version/identity).
Output: ProfileMetadata with format_version and calibration identity; profile-format.md with version, compatibility policy, and reproducibility section.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contracts-and-versioned-formats/01-CONTEXT.md
@.planning/phases/01-contracts-and-versioned-formats/01-RESEARCH.md
@crates/neurohid-types/src/profile.rs
@crates/neurohid-storage/src/profile.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add format_version and calibration identity to profile types and storage</name>
  <files>crates/neurohid-types/src/profile.rs, crates/neurohid-storage/src/profile.rs</files>
  <action>
In neurohid-types/profile.rs: Add `pub format_version: u32` to `ProfileMetadata` with `#[serde(default)]` (default 1). Add an optional reproducibility-identity field for calibration: e.g. `pub calibration_identity: Option<CalibrationIdentity>` where `CalibrationIdentity` is a small struct with `format_version: u32` and optional `content_hash: Option<String>` (or equivalent); or per RESEARCH minimal approach, add `calibration_format_version: Option<u32>` and optional hash field to ProfileMetadata so the same setup can be reproduced. Ensure `ProfileMetadata::new` sets format_version to 1 and identity fields to None. In neurohid-storage/profile.rs: when saving metadata, persist the new fields; when loading, deserialize with defaults. When `save_calibration` is called, update profile metadata with calibration identity (e.g. set calibration_format_version to 1 and optionally compute/store a content hash of the calibration blob). If updating metadata on every save_calibration is invasive, alternatively write a small calibration manifest file (e.g. calibration_manifest.json) alongside calibration.enc with version and optional hash — document that choice in profile-format.md. Prefer in-profile-metadata identity so export_profile/import_profile roundtrip it without extra files.
  </action>
  <verify>cargo check -p neurohid-types -p neurohid-storage; cargo test -p neurohid-storage -- profile; create profile, save calibration, export profile, reload metadata and assert format_version and calibration identity are present where expected.</verify>
  <done>ProfileMetadata has format_version; calibration identity is stored (in metadata or alongside calibration.enc) and roundtrips on export/import; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document profile format, compatibility, and reproducibility identity</name>
  <files>docs/formats/profile-format.md</files>
  <action>
Create docs/formats/ if needed. Write profile-format.md that (per user decision) keeps version and compatibility in the same doc: (1) format version — current profile metadata version and meaning; (2) compatibility policy — readers support at least N previous versions (N=2); breaking changes and migration policy; (3) schema or BNF for profile metadata; (4) Reproducibility identity — where identity is stored (in profile metadata; optionally alongside calibration.enc), what identifies "same setup" (version + profile id + optional calibration version/hash), and the two use cases: re-run with same config and audit/share (others can verify or reuse the exact setup). Use structured sections and brief prose.
  </action>
  <verify>File exists; grep for "format_version", "compatibility", "reproducibility", "identity", "re-run", "audit" in docs/formats/profile-format.md.</verify>
  <done>Profile format is documented in one place with version, compatibility policy, and reproducibility identity (where stored, how used for re-run and audit/share).</done>
</task>

</tasks>

<verification>
- cargo build --workspace succeeds.
- Profile metadata roundtrips with format_version; export/import preserves identity.
- docs/formats/profile-format.md exists and contains version, compatibility policy, and reproducibility identity section.
</verification>

<success_criteria>
- COMP-05 satisfied for profile: profile format is versioned with documented compatibility policy in the same doc.
- PATH-03 satisfied: calibration and profile metadata are stored with version/identity so the same setup can be reproduced; doc explains re-run and audit/share.
- Plan 02 SUMMARY created at .planning/phases/01-contracts-and-versioned-formats/01-02-SUMMARY.md after execution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-versioned-formats/01-02-SUMMARY.md`
</output>
