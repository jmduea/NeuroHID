---
phase: 01-contracts-and-versioned-formats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-types/src/config.rs
  - crates/neurohid-storage/src/config.rs
  - docs/formats/config-format.md
autonomous: true
requirements:
  - COMP-05
user_setup: []

must_haves:
  truths:
    - "Config format has a documented version and compatibility policy"
    - "Config files roundtrip with format_version present and readable by current code"
  artifacts:
    - path: docs/formats/config-format.md
      provides: "Config format version, compatibility policy, and schema/BNF in one doc"
      min_lines: 40
    - path: crates/neurohid-types/src/config.rs
      provides: "SystemConfig with format_version field"
      contains: "format_version"
    - path: crates/neurohid-storage/src/config.rs
      provides: "Config load/save with version handling"
  key_links:
    - from: crates/neurohid-storage/src/config.rs
      to: crates/neurohid-types/src/config.rs
      via: "serialize/deserialize SystemConfig with format_version"
      pattern: "format_version|SystemConfig"
    - from: docs/formats/config-format.md
      to: "COMP-05"
      via: "same doc contains version and compatibility policy"
      pattern: "compatibility|format_version|version"
---

<objective>
Add a format version to system config and document it with a compatibility policy in one place so config format evolution is explicit and readers can support N previous versions.

Purpose: Satisfy COMP-05 for config; enable reproducible setup by versioning the config artifact.
Output: SystemConfig with format_version; config-format.md with version, compatibility policy, and schema/BNF.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contracts-and-versioned-formats/01-CONTEXT.md
@.planning/phases/01-contracts-and-versioned-formats/01-RESEARCH.md
@crates/neurohid-types/src/config.rs
@crates/neurohid-storage/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add format_version to config types and storage</name>
  <files>crates/neurohid-types/src/config.rs, crates/neurohid-storage/src/config.rs</files>
  <action>
Add a top-level `format_version: u32` to system config. In neurohid-types: add `pub format_version: u32` to `SystemConfig` with `#[serde(default)]` so missing in existing files deserializes as 1. Set default in `Default` to 1. In neurohid-storage: no change to load/save logic required beyond serializing the new field; when loading, TOML without the key will get default 1 via serde(default). Ensure save uses current version (1) so new and updated configs persist the version. Do not add a separate envelope struct unless the codebase convention prefers it; RESEARCH recommends a single version field on the root struct.
  </action>
  <verify>cargo check -p neurohid-types -p neurohid-storage; cargo test -p neurohid-storage -- config; load a config file (or default), save, reload and assert format_version is present (or 1 for legacy).</verify>
  <done>SystemConfig has format_version; existing config files without the field load as version 1; save writes format_version; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document config format and compatibility policy</name>
  <files>docs/formats/config-format.md</files>
  <action>
Create docs/formats/ directory if needed. Write config-format.md that (per user decision) lives in the same doc: (1) format version — current version number and meaning; (2) compatibility policy — readers support at least N previous format versions (choose N=2; state it explicitly); breaking changes acceptable until external dependents exist, then document breaks and migration; (3) schema or BNF for the config format (brief; can reference SystemConfig shape or a small TOML BNF). Use structured style: clear sections and optional code block for schema. Do not put compatibility policy in a separate file.
  </action>
  <verify>File exists; grep for "format_version", "compatibility", and "version" in docs/formats/config-format.md; doc is readable and states N (e.g. 2) and where version appears in the file.</verify>
  <done>Config format is documented in one place with version and compatibility policy; N is stated.</done>
</task>

</tasks>

<verification>
- cargo build --workspace succeeds.
- Config roundtrip: load default or existing config, save, reload; format_version is 1 (or current).
- docs/formats/config-format.md exists and contains format version, compatibility policy, and schema/BNF.
</verification>

<success_criteria>
- COMP-05 satisfied for config: profile and config formats (config half) are versioned and have a documented compatibility policy in the same doc as the format spec.
- Plan 01 SUMMARY created at .planning/phases/01-contracts-and-versioned-formats/01-01-SUMMARY.md after execution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-and-versioned-formats/01-01-SUMMARY.md`
</output>
