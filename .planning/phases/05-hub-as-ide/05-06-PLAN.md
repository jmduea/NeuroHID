---
phase: 05-hub-as-ide
plan: 06
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - crates/neurohid-types/src/config.rs
  - crates/neurohid-hub/src/app.rs
  - crates/neurohid-hub/src/state.rs
  - docs/user-guide.md
autonomous: true
requirements:
  - HUB-05

must_haves:
  truths:
    - "User can follow one primary workflow: device setup → calibration → train decoder → run (embedded or external) without switching tools"
    - "Run choice (Run in Hub vs Run in background) is available from the Hub"
    - "When reopening the Hub, user picks up where they left off (resume state)"
  artifacts:
    - path: crates/neurohid-types/src/config.rs
      provides: UiConfig with optional last_screen or last_sidebar_id for resume
      contains: "last_screen|last_sidebar"
    - path: crates/neurohid-hub/src/app.rs
      provides: Apply resume state on startup; Run in Hub vs background in UI
      contains: "last_screen|resume|ServiceRuntimeMode|Embedded|External"
  key_links:
    - from: crates/neurohid-hub/src/app.rs
      to: state.config.ui
      via: load config and apply last_screen on init
      pattern: "last_screen|restore.*screen"
---

<objective>
Deliver HUB-05: user can follow one primary workflow in the Hub — device setup → calibration → train decoder → run (embedded or external) — without switching tools. Ensure "Run in Hub" (embedded) and "Run in background" (external) are both available and clear in the UI; add resume state (last open view or first incomplete step) so reopening the Hub restores context.

Purpose: Single workflow without switching tools; resume for returning users.
Output: UiConfig resume field(s); app applies them on startup; Run mode choice explicit in Hub; optional workflow hint in UI or docs.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hub-as-ide/05-CONTEXT.md
@.planning/phases/05-hub-as-ide/05-RESEARCH.md
@crates/neurohid-types/src/config.rs
@crates/neurohid-hub/src/app.rs
@docs/user-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resume state to UiConfig and apply on startup</name>
  <files>crates/neurohid-types/src/config.rs, crates/neurohid-hub/src/app.rs, crates/neurohid-hub/src/state.rs</files>
  <action>Add an optional field to UiConfig for resume: e.g. last_screen (Screen identifier or string) or last_sidebar_id (lane + screen). Serialize/deserialize with serde default so existing configs still load. On Hub startup, after loading config, set current_screen (and lane if applicable) from this field so the user "picks up where they left off." When the user changes screen (or on periodic save), persist the current screen/lane back to config. Use existing config load/save path; do not introduce a new persistence layer.</action>
  <verify>cargo check --workspace; run Hub, switch to Training, close and reopen Hub and confirm Training (or last screen) is shown</verify>
  <done>Reopening Hub opens to last open view (resume state).</done>
</task>

<task type="auto">
  <name>Task 2: Run in Hub vs Run in background explicit in UI</name>
  <files>crates/neurohid-hub/src/app.rs, crates/neurohid-hub/src/screens/dashboard.rs, crates/neurohid-hub/src/screens/settings.rs</files>
  <action>Ensure the user can choose "Run in Hub" (embedded runtime) vs "Run in background" (external neurohid-service). Per CONTEXT both must be available from the Hub. Use ServiceRuntimeMode::Embedded vs External; expose in Settings or Dashboard (e.g. runtime mode selector). When External is selected, document or show that user must start neurohid-service separately; Hub still shows status/snapshot via control polling. Verify embedded and external both work with snapshot and control.</action>
  <verify>cargo check -p neurohid-hub; run Hub, switch runtime mode, confirm both paths show status when running</verify>
  <done>User can select Run in Hub (embedded) or Run in background (external); both paths work.</done>
</task>

<task type="auto">
  <name>Task 3: Primary workflow documented or suggested in UI</name>
  <files>docs/user-guide.md, crates/neurohid-hub/src/app.rs</files>
  <action>Document the primary workflow (device setup → calibration → train decoder → run) in docs/user-guide.md under a "Hub workflow" or "Standard path in the Hub" section. Optionally add a short in-app hint (e.g. on Dashboard or first launch) suggesting the order: Devices → Calibration → Training → Run. Per CONTEXT, suggested order in docs or UI, not enforced. Keep free navigation.</action>
  <verify>Read docs/user-guide.md for Hub workflow section; optional: run Hub and see hint if added</verify>
  <done>Primary workflow is documented and optionally suggested in UI; navigation remains free.</done>
</task>

</tasks>

<verification>
- cargo check --workspace passes.
- Resume: last screen restored on reopen.
- Run in Hub vs Run in background selectable and both functional.
- User guide describes Hub workflow.
</verification>

<success_criteria>
- HUB-05 satisfied: one primary workflow (device → calibration → train → run) without switching tools; Run choice (embedded/external) and resume state implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-as-ide/05-06-SUMMARY.md`
</output>
