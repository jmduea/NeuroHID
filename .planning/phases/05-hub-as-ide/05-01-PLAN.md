---
phase: 05-hub-as-ide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-hub/src/workbench.rs
  - crates/neurohid-hub/src/app.rs
  - crates/neurohid-hub/src/screens/mod.rs
  - crates/neurohid-hub/src/screens/training.rs
autonomous: true
requirements:
  - HUB-05

must_haves:
  truths:
    - "Sidebar shows top-level entries: Devices, Calibration, Training, Visualization (plus Config/others per CONTEXT)"
    - "Training is a selectable screen from the sidebar"
    - "Navigation is free (user can open any screen anytime)"
  artifacts:
    - path: crates/neurohid-hub/src/workbench.rs
      provides: Lane/screen mapping aligned to CONTEXT (Calibration, Training, Visualization, Devices)
      contains: "screens_for_lane"
    - path: crates/neurohid-hub/src/screens/mod.rs
      provides: Screen enum including Training
      contains: "Training"
    - path: crates/neurohid-hub/src/screens/training.rs
      provides: Training screen stub
      min_lines: 20
  key_links:
    - from: crates/neurohid-hub/src/app.rs
      to: workbench/screens
      via: sidebar rendering and current_screen routing
      pattern: "visible_screens|current_screen"
---

<objective>
Align Hub sidebar and workbench to Phase 5 CONTEXT: three primary entries (Calibration, Training, Visualization), dedicated Devices, and add a Training screen stub so the primary workflow can be followed without switching tools.

Purpose: Foundation for HUB-05 (one primary workflow in the Hub). All other phase plans depend on this nav structure.
Output: Updated ActivityLane/screens_for_lane/lane_for_screen, Screen enum with Training, training.rs stub, app.rs routing to Training screen.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hub-as-ide/05-CONTEXT.md
@.planning/phases/05-hub-as-ide/05-RESEARCH.md
@crates/neurohid-hub/src/workbench.rs
@crates/neurohid-hub/src/screens/mod.rs
@crates/neurohid-hub/src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize lanes and screen mapping for CONTEXT</name>
  <files>crates/neurohid-hub/src/workbench.rs</files>
  <action>Update ActivityLane and screens_for_lane/lane_for_screen so the sidebar has top-level entries matching CONTEXT: (1) Devices (dedicated screen), (2) Calibration, (3) Training, (4) Visualization. Keep Dashboard, Profiles, Settings, and Labs (Jupyter/Python) in a sensible grouping (e.g. Config or Ops) per RESEARCH "placement at implementer's discretion." Add Training to the Screen enum in screens/mod.rs and to all_for_mode (both Standard and Advanced) and to the lane that will show the Training screen. Ensure lane_for_screen(Screen::Training) returns the correct lane. Do not remove any existing screens; only add Training and reorder/regroup lanes so Calibration, Training, Visualization are clearly top-level alongside Devices.</action>
  <verify>cargo check -p neurohid-hub; grep -n "Training\|screens_for_lane\|ActivityLane" crates/neurohid-hub/src/workbench.rs crates/neurohid-hub/src/screens/mod.rs</verify>
  <done>workbench.rs compiles; Screen::Training exists; screens_for_lane returns Training in the intended lane; lane_for_screen(Screen::Training) is correct.</done>
</task>

<task type="auto">
  <name>Task 2: Add Training screen stub and route in app</name>
  <files>crates/neurohid-hub/src/screens/training.rs, crates/neurohid-hub/src/screens/mod.rs, crates/neurohid-hub/src/app.rs</files>
  <action>Create crates/neurohid-hub/src/screens/training.rs with a minimal TrainingScreen struct (Default, show method that draws a placeholder message e.g. "Training — config and progress"). Export it from screens/mod.rs. In app.rs, add a match arm (or equivalent) so that when current_screen is Screen::Training, the central content area renders the Training screen (same pattern as calibration.rs or devices.rs). Use existing HubState and ServiceManager in show signature if needed.</action>
  <verify>cargo check -p neurohid-hub; cargo test -p neurohid-hub</verify>
  <done>Training screen is reachable from sidebar and renders placeholder content; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update sidebar shell to show new lane order</name>
  <files>crates/neurohid-hub/src/app.rs</files>
  <action>Ensure the sidebar (render_sidebar_shell or equivalent) displays lanes in the order that matches CONTEXT: Devices, Calibration, Training, Visualization, then Config/others. Use ActivityLane::ALL or the order used by visible_screens so that the three primary items (Calibration, Training, Visualization) and Devices are clearly visible. Update any lane ordering or labels if current code uses Ops/Analysis/Labs/Config so that the new lane names/order reflect the reorganized workbench.</action>
  <verify>cargo check -p neurohid-hub; manual inspection that sidebar lists Calibration, Training, Visualization, Devices</verify>
  <done>Sidebar shows Calibration, Training, Visualization, and Devices as top-level; clicking each switches to the correct screen.</done>
</task>

</tasks>

<verification>
- cargo check --workspace and cargo test -p neurohid-hub pass.
- Sidebar shows Devices, Calibration, Training, Visualization (and remaining lanes); selecting Training shows the stub Training screen.
</verification>

<success_criteria>
- Lanes and screens align with 05-CONTEXT.md (Calibration, Training, Visualization as primary; Devices dedicated screen).
- Training screen exists and is reachable; no regressions in existing screen routing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-as-ide/05-01-SUMMARY.md`
</output>
