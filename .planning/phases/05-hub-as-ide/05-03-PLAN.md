---
phase: 05-hub-as-ide
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/neurohid-hub/src/screens/calibration.rs
  - crates/neurohid-calibration/src/lib.rs
  - crates/neurohid-calibration/src/panel.rs
autonomous: true
requirements:
  - HUB-02

must_haves:
  truths:
    - "User sees a list or grid of calibration games and picks one to run"
    - "User runs calibration with wizard-style steps before each game"
    - "Calibration results are tied to the current profile for reproducibility"
  artifacts:
    - path: crates/neurohid-hub/src/screens/calibration.rs
      provides: Game list/grid entrypoint and wrapper for panel
      contains: "game|GridMaze|TargetTracking|wizard"
    - path: crates/neurohid-hub/src/screens/calibration.rs
      provides: Persist calibration to active profile
      contains: "persist_calibration_outputs|active_profile"
  key_links:
    - from: crates/neurohid-hub/src/screens/calibration.rs
      to: neurohid_calibration::CalibrationPanel
      via: panel per selected game
      pattern: "CalibrationPanel|CalibrationPanelResult"
---

<objective>
Deliver HUB-02: user can run calibration (wizard/games) from the Hub with results tied to a profile. Add a game list or grid as the Calibration top-level entrypoint; user picks a game (e.g. Grid Maze, Target Tracking), then wizard then run that game's panel. Persist outputs to active profile.

Purpose: Calibration as game picker + wizard + run, with profile identity for reproducibility.
Output: Calibration screen shows list/grid of games; selecting one runs wizard (if applicable) then that game; calibration results persisted to current profile.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hub-as-ide/05-CONTEXT.md
@.planning/phases/05-hub-as-ide/05-RESEARCH.md
@crates/neurohid-hub/src/screens/calibration.rs
@crates/neurohid-calibration/src/panel.rs
@crates/neurohid-calibration/src/games/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibration game list/grid entrypoint</name>
  <files>crates/neurohid-hub/src/screens/calibration.rs</files>
  <action>Add a game list or grid as the default view when the user opens the Calibration screen. Show available games (e.g. Grid Maze, Target Tracking) as selectable items. When no game is selected, show this list/grid; when user selects a game, show wizard steps (mandatory steps/explanations per CONTEXT) then launch that game's CalibrationPanel (existing neurohid-calibration panel). Reuse CalibrationPanel and CalibrationPanelResult; do not reimplement games. Optional: show a placeholder for "decoder accuracy for this game" (e.g. "Accuracy: —" or "N/A") when backend does not yet provide it. One "current profile" from Hub state; calibration games always write to that profile.</action>
  <verify>cargo check -p neurohid-hub -p neurohid-calibration; run Hub, open Calibration, see game list and select a game to run</verify>
  <done>Calibration top-level shows list/grid of games; selecting one runs wizard then that game's panel.</done>
</task>

<task type="auto">
  <name>Task 2: Wizard before game and persist to profile</name>
  <files>crates/neurohid-hub/src/screens/calibration.rs</files>
  <action>Ensure mandatory wizard-style steps or explanations are shown before each game or phase (CONTEXT). After a game completes (CalibrationPanelResult::Completed), persist calibration outputs to the active profile via existing persist_calibration_outputs using state.active_profile_id, profile_store, and config. Confirm that calibration results are tied to profile/identity so the same setup can be reproduced (HUB-02).</action>
  <verify>cargo test -p neurohid-hub; run calibration flow, confirm profile has updated calibration data</verify>
  <done>Wizard steps appear before game; completion persists to active profile; results tied to profile identity.</done>
</task>

</tasks>

<verification>
- cargo check --workspace and cargo test -p neurohid-hub pass.
- Calibration: game list → pick game → wizard → run → persist to profile.
</verification>

<success_criteria>
- HUB-02 satisfied: run calibration (wizard/games) from the Hub; results tied to profile/identity for reproducibility.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-as-ide/05-03-SUMMARY.md`
</output>
