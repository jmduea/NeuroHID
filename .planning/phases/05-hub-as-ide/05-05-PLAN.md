---
phase: 05-hub-as-ide
plan: 05
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/neurohid-hub/src/screens/visualization.rs
  - crates/neurohid-hub/src/layout.rs
  - crates/neurohid-hub/src/workbench.rs
  - crates/neurohid-hub/src/app.rs
autonomous: true
requirements:
  - HUB-04

must_haves:
  truths:
    - "User can open Visualization anytime and see real-time signal and pipeline state"
    - "User can customize visualization (add/remove/arrange panels)"
    - "During Run, user can have Run and Visualization open together (e.g. side by side or tabs)"
  artifacts:
    - path: crates/neurohid-hub/src/screens/visualization.rs
      provides: Customizable panels and real-time data
      contains: "LayoutManager|egui_dock|DataBus|snapshot"
    - path: crates/neurohid-hub/src/layout.rs
      provides: Layout preset and pane widget persistence
      contains: "visualization_layout_preset|visualization_pane_widgets"
  key_links:
    - from: crates/neurohid-hub/src/screens/visualization.rs
      to: state.service_snapshot / DataBus
      via: real-time signal and pipeline state
      pattern: "signal|features|decoder"
---

<objective>
Deliver HUB-04: user can visualize real-time signal and pipeline state (e.g. features, decoder output) in the Hub during experiments. Visualization is a separate view open anytime; customizable (add/remove/arrange panels); and during Run, user can have Run and Visualization open together (tabs or side-by-side).

Purpose: One place for real-time visualization during experiments.
Output: Visualization screen remains customizable; Run and Visualization can be open together; layout persisted via UiConfig.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hub-as-ide/05-CONTEXT.md
@.planning/phases/05-hub-as-ide/05-RESEARCH.md
@crates/neurohid-hub/src/screens/visualization.rs
@crates/neurohid-hub/src/layout.rs
@crates/neurohid-types/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure Visualization is customizable and open anytime</name>
  <files>crates/neurohid-hub/src/screens/visualization.rs, crates/neurohid-hub/src/layout.rs</files>
  <action>Confirm the Visualization screen supports add/remove/arrange panels (egui_dock and LayoutManager). Persist layout (visualization_layout_preset, visualization_pane_widgets) to UiConfig via take_persisted_state / from_ui_config. Ensure user can open Visualization from the sidebar from any lane/screen (already true if Visualization is a top-level entry from 05-01). No new GUI stack; use existing egui_dock and layout.rs patterns.</action>
  <verify>cargo check -p neurohid-hub; run Hub, open Visualization, add/remove/reorder panes and confirm persistence after restart</verify>
  <done>Visualization is open anytime from sidebar; layout is customizable and persisted.</done>
</task>

<task type="auto">
  <name>Task 2: Run and Visualization open together</name>
  <files>crates/neurohid-hub/src/app.rs, crates/neurohid-hub/src/workbench.rs</files>
  <action>Ensure that during "Run" (Dashboard or runtime-active state), the user can have the Run view and the Visualization view accessible together. Options: (1) both as tabs in the same content area when switching screens, or (2) side-by-side if the app supports split content. CONTEXT says "during Run they can have Run and Visualization open together (e.g. side by side or tabs)." Implement the minimal approach: e.g. user can switch to Visualization while runtime is running and see live data; or support both Dashboard and Visualization in the same lane so they can switch without leaving "run" context. Document the chosen behavior if ambiguous.</action>
  <verify>cargo check -p neurohid-hub; run Hub, start runtime, open Visualization and confirm both Run (Dashboard) and Visualization are usable (switch or side-by-side)</verify>
  <done>User can view Run and Visualization together during experiments (tabs or side-by-side as implemented).</done>
</task>

</tasks>

<verification>
- cargo check --workspace passes.
- Visualization: open anytime, customizable panels, layout persisted.
- Run and Visualization can be used together during experiments.
</verification>

<success_criteria>
- HUB-04 satisfied: visualize real-time signal and pipeline state in the Hub during experiments; customizable; Run and Visualization open together when needed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-as-ide/05-05-SUMMARY.md`
</output>
