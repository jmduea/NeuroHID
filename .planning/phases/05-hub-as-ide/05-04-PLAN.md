---
phase: 05-hub-as-ide
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/neurohid-hub/src/screens/training.rs
  - crates/neurohid-hub/src/app.rs
  - crates/neurohid-hub/src/service_manager.rs
autonomous: true
requirements:
  - HUB-03

must_haves:
  truths:
    - "User can configure decoder training (e.g. model path, params, profile/dataset) from the Hub"
    - "User can launch decoder training from the Hub"
    - "User can observe training progress and metrics in the Hub (live)"
  artifacts:
    - path: crates/neurohid-hub/src/screens/training.rs
      provides: Split layout — config/setup pane and live progress/metrics pane
      min_lines: 80
    - path: crates/neurohid-hub/src/service_manager.rs
      provides: Trainer snapshot for live panel
      contains: "trainer_snapshot|TrainerSnapshot"
  key_links:
    - from: crates/neurohid-hub/src/screens/training.rs
      to: ServiceSnapshot / TrainerSnapshot
      via: state.service_snapshot and service_manager.trainer_snapshot()
      pattern: "trainer_step|trainer_policy_loss|trainer_value_loss|trainer_last_error"
---

<objective>
Deliver HUB-03: user can configure and launch decoder training from the Hub and observe training progress and metrics. Replace the Training screen stub with a split layout: one side for config/setup (model path, params, profile/dataset, optional "train on existing data" trigger), other side for live progress and metrics from ControlSnapshot trainer_* and TrainerSnapshot.

Purpose: Training view is the single place to configure and observe decoder training.
Output: Training screen with split layout (config | live progress), wired to ServiceManager and snapshot.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hub-as-ide/05-CONTEXT.md
@.planning/phases/05-hub-as-ide/05-RESEARCH.md
@crates/neurohid-hub/src/screens/training.rs
@crates/neurohid-hub/src/service_manager.rs
@crates/neurohid-types/src/control.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Training screen split layout — config pane</name>
  <files>crates/neurohid-hub/src/screens/training.rs</files>
  <action>Implement the left (or top) pane of the Training screen: config/setup area. Show controls for model path, decoder/profile or dataset selection, and training parameters as exposed by neurohid-types config or control. Add a button or action to trigger training on existing profile data (e.g. "Train on collected data" / "Retrain"). If the control protocol does not yet expose a "start training" command, add a stub and document as follow-up per RESEARCH. Use HubState and ServiceManager; read config from state.config.</action>
  <verify>cargo check -p neurohid-hub; open Training screen and see config pane with model path and trigger</verify>
  <done>Config pane shows model path, params, profile/dataset, and a way to trigger training on existing data (or stub).</done>
</task>

<task type="auto">
  <name>Task 2: Training screen split layout — live progress pane</name>
  <files>crates/neurohid-hub/src/screens/training.rs, crates/neurohid-hub/src/service_manager.rs</files>
  <action>Implement the right (or bottom) pane: live training progress and metrics. Use ControlSnapshot fields (trainer_replay_size, trainer_step, trainer_policy_loss, trainer_value_loss, trainer_entropy, trainer_last_error) and optionally TrainerSnapshot from service_manager.trainer_snapshot(). Poll or update from existing snapshot flow. Display step, loss values, and last error so the user can observe training in real time. Calibration auto-starts training; this pane shows progress when training is running (from calibration or from the config-pane trigger).</action>
  <verify>cargo check -p neurohid-hub; run Hub with embedded runtime and calibration or training, confirm Training screen shows live metrics</verify>
  <done>Live pane shows trainer step, losses, and errors when training is active.</done>
</task>

<task type="auto">
  <name>Task 3: Wire trainer_snapshot in ServiceManager if missing</name>
  <files>crates/neurohid-hub/src/service_manager.rs</files>
  <action>Ensure ServiceManager exposes trainer_snapshot(): for embedded runtime call rt.trainer_snapshot(); for external runtime send ControlCommand::TrainerSnapshot and handle ControlResponsePayload::TrainerSnapshot. If already implemented, verify and ensure Training screen calls it for the live pane. Do not add a new protocol; use existing ControlSnapshot and TrainerSnapshot types.</action>
  <verify>grep -n "trainer_snapshot|TrainerSnapshot" crates/neurohid-hub/src/service_manager.rs; cargo check -p neurohid-hub</verify>
  <done>trainer_snapshot() is available and used by the Training screen for live metrics.</done>
</task>

</tasks>

<verification>
- cargo check --workspace passes.
- Training screen has two areas: config/setup and live progress/metrics.
- Live pane updates from snapshot when training is running.
</verification>

<success_criteria>
- HUB-03 satisfied: configure and launch decoder training from the Hub; observe training progress and metrics in the Hub.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-as-ide/05-04-SUMMARY.md`
</output>
