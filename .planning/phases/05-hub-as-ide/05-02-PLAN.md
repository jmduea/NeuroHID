---
phase: 05-hub-as-ide
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/neurohid-hub/src/app.rs
  - crates/neurohid-hub/src/screens/devices.rs
autonomous: true
requirements:
  - HUB-01

must_haves:
  truths:
    - "User can discover and connect devices from the Hub in one place"
    - "User sees connection status and stream health in one place (Devices screen and/or persistent strip)"
  artifacts:
    - path: crates/neurohid-hub/src/screens/devices.rs
      provides: Device discovery, connect/disconnect, stream health
      contains: "discovered_streams|ConnectStream|DisconnectStream|RescanStreams"
    - path: crates/neurohid-hub/src/app.rs
      provides: Status bar or strip with device/stream at-a-glance
      contains: "Devices|signal_quality|status_bar|show_status"
  key_links:
    - from: crates/neurohid-hub/src/screens/devices.rs
      to: ServiceManager/ControlSnapshot
      via: snapshot.discovered_streams and control commands
      pattern: "rescan_streams|send_control_request|ConnectStream|DisconnectStream"
---

<objective>
Deliver HUB-01: user can discover and connect devices from the Hub and see connection status and stream health in one place. Ensure the Devices screen and a persistent at-a-glance strip (status bar or equivalent) are the single place for this.

Purpose: One place for device/stream status so users don't switch tools.
Output: Devices screen with discover/connect/disconnect and stream health; persistent strip (or status bar) showing device count and signal health from anywhere.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hub-as-ide/05-CONTEXT.md
@.planning/phases/05-hub-as-ide/05-RESEARCH.md
@crates/neurohid-hub/src/screens/devices.rs
@crates/neurohid-hub/src/service_manager.rs
@crates/neurohid-types/src/control.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Devices screen — discover, connect, disconnect, stream health</name>
  <files>crates/neurohid-hub/src/screens/devices.rs</files>
  <action>Ensure the Devices screen shows ControlSnapshot.discovered_streams with connection status (connected/not), stream identity, and stream health (e.g. channel_quality, integrity_state from DiscoveredStream). Provide RescanStreams, ConnectStream(stream_id), and DisconnectStream(stream_id) via ServiceManager (rescan_streams, send_control_request with ControlCommand). Use existing snapshot-driven pattern; do not duplicate discovery logic. If the screen already implements this, verify and tighten labels/layout so "one place" is clear; add any missing connect/disconnect or health column.</action>
  <verify>cargo check -p neurohid-hub; run Hub, open Devices, confirm list and connect/disconnect/rescan behavior</verify>
  <done>Devices screen lists discovered streams, shows connection and health, and supports rescan/connect/disconnect.</done>
</task>

<task type="auto">
  <name>Task 2: Persistent device/stream strip visible from anywhere</name>
  <files>crates/neurohid-hub/src/app.rs</files>
  <action>Ensure a persistent at-a-glance strip (e.g. status bar) is visible from every screen showing device count (connected/total) and signal quality (e.g. snapshot.signal_quality). RESEARCH notes status bar already shows "Devices X/Y" and "Signal %"; formalize or enhance so it is clearly the persistent strip per CONTEXT. Use ServiceSnapshot (ControlSnapshot) as single source of truth; no parallel hub-specific state for streams.</action>
  <verify>cargo check -p neurohid-hub; run Hub, switch between Calibration/Training/Visualization/Devices and confirm strip shows same device/signal info</verify>
  <done>From any screen, user sees device count and signal health in one persistent strip (status bar).</done>
</task>

</tasks>

<verification>
- cargo check --workspace passes.
- Devices screen: discover, connect, disconnect, stream health visible.
- Status bar (or equivalent) shows Devices X/Y and Signal % from any screen.
</verification>

<success_criteria>
- HUB-01 satisfied: discover and connect devices from the Hub; connection status and stream health visible in one place (Devices + persistent strip).
</success_criteria>

<output>
After completion, create `.planning/phases/05-hub-as-ide/05-02-SUMMARY.md`
</output>
