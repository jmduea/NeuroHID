---
phase: 09-brainflow-first-class
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - crates/neurohid-core/src/tasks/device/discovery.rs
  - crates/neurohid-core/src/runtime.rs
  - crates/neurohid/src/bin/neurohid-service.rs
  - crates/neurohid-sdk/src/lib.rs
  - crates/neurohid-hub/src/service_manager/tests.rs
  - crates/neurohid/src/bin/neurohid-validate.rs
autonomous: true
requirements:
  - BRAIN-05
user_setup: []

must_haves:
  truths:
    - "BrainFlow's synthetic board is the single non-hardware device path: tests, examples, and CI use it; no separate mock backend in user-facing paths"
    - "Auto backend falls back to BrainFlow synthetic (board_id 0), not Mock"
  artifacts:
    - path: crates/neurohid-core/src/tasks/device/discovery.rs
      provides: "AutoProvider uses BrainFlow synthetic as fallback; create_provider(Auto) returns LSL + BrainFlow fallback"
      contains: "BrainFlowProvider"
  key_links:
    - from: discovery.rs Auto branch
      to: BrainFlowProvider
      via: "create_brainflow_provider with default config (board_id 0)"
      pattern: "brainflow|BrainFlow"
    - from: All Mock-using tests
      to: DeviceBackend::BrainFlow + BrainFlowConfig
      via: "Replace Mock with BrainFlow synthetic in test configs"
      pattern: "BrainFlow"
---

<objective>
Make BrainFlow synthetic the single non-hardware device path: Auto backend falls back to BrainFlow synthetic instead of Mock; all tests, CI, and remaining examples use BrainFlow synthetic (board_id 0) instead of Mock. Mock remains available as DeviceBackend::Mock for internal use but is no longer used in default paths, tests, or CI (BRAIN-05). Depends on 09-01 (brainflow in default build).
Purpose: Synthetic board fully replaces in-repo mock as the canonical non-hardware path.
Output: Auto fallback = BrainFlow; all relevant tests and binaries use BrainFlow synthetic; no separate mock backend in user-facing or CI paths.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-brainflow-first-class/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto fallback to BrainFlow synthetic</name>
  <files>crates/neurohid-core/src/tasks/device/discovery.rs</files>
  <action>
    In create_provider, when backend is DeviceBackend::Auto, create a BrainFlow provider with default config (config.brainflow.clone().unwrap_or_default() — default is board_id 0) instead of MockProvider. Use create_brainflow_provider(config) so the same config is used if user set brainflow options. Replace the mock field in AutoProvider with this fallback provider (type remains Box&lt;dyn DeviceProvider&gt;). Update AutoProvider::new(lsl, fallback) to take the BrainFlow provider. In AutoProvider discover(): when LSL returns empty or error, call self.fallback.discover() (same as today but fallback is BrainFlow). In connect(): when device_id does not match LSL, try fallback.connect() (remove or generalize the mock_ prefix check — BrainFlow ids are like "brainflow::0::auto"; so try LSL first, on failure try fallback). Update log messages from "Mock" to "BrainFlow synthetic" or "fallback" (e.g. "Auto: no LSL streams found, falling back to BrainFlow synthetic"). Keep DeviceBackend::Mock branch unchanged (still returns MockProvider) so Mock remains available for code that explicitly requests it. Ensure #[cfg(feature = "brainflow")] guards for create_brainflow_provider and BrainFlowProvider are satisfied (brainflow is now in default features per 09-01).
  </action>
  <verify>cargo build -p neurohid-core; cargo test -p neurohid-core (if any discovery tests); manual: set backend Auto, no LSL, Rescan — should discover BrainFlow synthetic device.</verify>
  <done>Auto backend uses BrainFlow synthetic as fallback; log messages reference BrainFlow synthetic; Mock branch still present for explicit Mock use.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Mock with BrainFlow synthetic in all tests and binaries</name>
  <files>crates/neurohid-core/src/runtime.rs, crates/neurohid/src/bin/neurohid-service.rs, crates/neurohid-sdk/src/lib.rs, crates/neurohid-hub/src/service_manager/tests.rs, crates/neurohid/src/bin/neurohid-validate.rs</files>
  <action>
    Replace every test and example usage of DeviceBackend::Mock (and MockProvider/MockDeviceConfig where constructed directly) with DeviceBackend::BrainFlow and BrainFlowConfig { board_id: 0, serial_port: None } (or BrainFlowConfig::default()). Files to update: (1) neurohid-core/src/runtime.rs — tests that set config.device.backend = DeviceBackend::Mock: set backend = BrainFlow, config.device.brainflow = Some(BrainFlowConfig::default()). (2) neurohid/src/bin/neurohid-service.rs — all test configs that set DeviceBackend::Mock: same change. (3) neurohid-sdk/src/lib.rs — tests that use MockProvider or DeviceBackend::Mock: use BrainFlow provider or config (BrainFlowProvider::new(BrainFlowConfig::default()) and backend BrainFlow; ensure neurohid-sdk tests that need a device use the device-brainflow feature or that the runtime under test has brainflow, which is default in neurohid-core). (4) neurohid-hub/src/service_manager/tests.rs — config.device.backend = DeviceBackend::Mock to BrainFlow with brainflow config. (5) neurohid/src/bin/neurohid-validate.rs — any test or validation path that sets Mock to BrainFlow. Do not remove or change neurohid-device/src/mock.rs or its tests (mock type remains for internal use and BrainFlow adapter internals). Do not remove DeviceBackend::Mock from neurohid-types; only stop using it in tests and default paths. After changes, run cargo test --workspace and fix any failures (e.g. device id expectations from "mock_*" to "brainflow::0::auto" or similar).
  </action>
  <verify>cargo test --workspace passes; no test sets DeviceBackend::Mock except possibly neurohid-device mock unit tests; grep -r "DeviceBackend::Mock" crates --include="*.rs" shows only enum definition, discovery branch, and optionally device mock tests.</verify>
  <done>All runtime, service, SDK, Hub, and validate tests use BrainFlow synthetic; workspace tests pass; Mock is no longer the non-hardware path in tests or CI.</done>
</task>

</tasks>

<verification>
- cargo test --workspace
- cargo build -p neurohid
- Auto backend fallback is BrainFlow synthetic; discovery.rs logs say "BrainFlow synthetic"
- No remaining test use of Mock for device backend except mock.rs unit tests
</verification>

<success_criteria>
- Auto fallback is BrainFlow synthetic (board_id 0); Mock is not used in Auto path.
- Tests, examples, and CI use BrainFlow synthetic as the single non-hardware path; BRAIN-05 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/09-brainflow-first-class/09-03-SUMMARY.md`
</output>
