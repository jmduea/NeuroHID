---
phase: 09-brainflow-first-class
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - crates/neurohid-sdk/examples/embedded_runtime.rs
  - crates/neurohid-hub/src/screens/devices.rs
  - crates/neurohid-hub/src/screens/settings/mod.rs
  - crates/neurohid-hub/src/screens/settings/device.rs
autonomous: true
requirements:
  - BRAIN-02
  - BRAIN-03
user_setup: []

must_haves:
  truths:
    - "User can run at least one runnable example using BrainFlow's synthetic board end-to-end"
    - "User can discover and connect BrainFlow devices from the Hub Devices screen with UX parity to LSL (discover, connect, disconnect)"
  artifacts:
    - path: crates/neurohid-sdk/examples/embedded_runtime.rs
      provides: "Runnable example using BrainFlow synthetic (board_id 0) with discover→connect→stream"
      contains: "DeviceBackend::BrainFlow"
    - path: crates/neurohid-hub/src/screens/devices.rs
      provides: "Devices screen copy includes BrainFlow; no LSL-only or parity-planned messaging"
  key_links:
    - from: embedded_runtime example
      to: neurohid-core create_provider
      via: "RuntimeBuilder with config.device.backend = BrainFlow, brainflow = Some(BrainFlowConfig)"
      pattern: "BrainFlow"
    - from: Hub Devices screen
      to: same discover/connect/disconnect flow
      via: "Backend chosen in Settings; Rescan/Connect/Disconnect use same commands for BrainFlow"
      pattern: "Streams"
---

<objective>
Deliver one runnable example that uses BrainFlow synthetic board end-to-end (BRAIN-02) and update Hub Devices screen copy and Settings messaging so BrainFlow has UX parity with LSL — discover, connect, disconnect work the same; copy no longer implies LSL-only or "parity planned" (BRAIN-03). Depends on 09-01 so brainflow is in default build.
Purpose: Runnable demo and Hub UX parity for BrainFlow.
Output: embedded_runtime (or equivalent) uses BrainFlow synthetic; Devices screen and Settings copy updated.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-brainflow-first-class/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Runnable example with BrainFlow synthetic</name>
  <files>crates/neurohid-sdk/examples/embedded_runtime.rs</files>
  <action>
    Change the embedded_runtime example from Mock to BrainFlow synthetic so it demonstrates BrainFlow with NeuroHID end-to-end. Set config.device.backend = DeviceBackend::BrainFlow and config.device.brainflow = Some(BrainFlowConfig { board_id: 0, serial_port: None }) (or BrainFlowConfig::default() which is board_id 0). Ensure the example still runs with the same feature set (runtime, types); after 09-01, neurohid-core default includes brainflow so the runtime used by the example will have BrainFlow. Add a brief comment that this uses BrainFlow synthetic board (no hardware). Do not add a new example crate; use embedded_runtime as the one runnable example. Run command remains: cargo run -p neurohid-sdk --example embedded_runtime --features "runtime,types".
  </action>
  <verify>cargo run -p neurohid-sdk --example embedded_runtime --features "runtime,types" runs without error; example output shows device_connected or streams discovered; grep -E "BrainFlow|brainflow" crates/neurohid-sdk/examples/embedded_runtime.rs</verify>
  <done>At least one runnable example uses BrainFlow synthetic and runs successfully with default-feature build.</done>
</task>

<task type="auto">
  <name>Task 2: Hub Devices screen and Settings copy for BrainFlow parity</name>
  <files>crates/neurohid-hub/src/screens/devices.rs, crates/neurohid-hub/src/screens/settings/mod.rs, crates/neurohid-hub/src/screens/settings/device.rs</files>
  <action>
    Update Hub copy so BrainFlow is presented as a peer to LSL (discover, connect, disconnect already use the same flow; gap is messaging). In devices.rs: (1) Change "Start service to discover/connect LSL streams" to wording that includes BrainFlow (e.g. "Start service to discover/connect streams (LSL, BrainFlow, …)"). (2) Change heading "Available Streams (LSL-first)" to "Available Streams" or "Available Streams (LSL / BrainFlow / …)" so it is not LSL-only. (3) Remove or replace the status chip "LSL-first scope". (4) Replace "Ensure device software is pushing to LSL; use Rescan to check manually" with wording that allows BrainFlow (e.g. "Use Rescan to discover streams; LSL requires a publisher; BrainFlow synthetic appears when backend is BrainFlow."). (5) Remove the chip "Serial/BrainFlow parity is planned in later phases" — parity is delivered in Phase 9. In settings/mod.rs: remove or reword "LSL-first telemetry UX" and the "Serial/BrainFlow parity is phased later" chip when backend is Serial or BrainFlow (BrainFlow parity is now in place). In settings/device.rs: if there is copy that says BrainFlow parity is phased later, remove or update it so BrainFlow is first-class. Do not change the discovery/connect/disconnect implementation; only copy and status chips so UX parity is reflected in the UI text.
  </action>
  <verify>Build neurohid-hub and neurohid; grep for "LSL-first" and "parity is planned" and "parity is phased" in crates/neurohid-hub — should be removed or replaced; Devices screen shows neutral or multi-backend wording.</verify>
  <done>Devices screen and Settings no longer imply LSL-only or that BrainFlow parity is planned; user can discover/connect BrainFlow from Hub with same flow as LSL.</done>
</task>

</tasks>

<verification>
- cargo run -p neurohid-sdk --example embedded_runtime --features "runtime,types" succeeds
- cargo build -p neurohid
- No "LSL-first" or "parity is planned/phased" in Hub copy for BrainFlow; Devices heading and chips updated
</verification>

<success_criteria>
- One runnable example uses BrainFlow synthetic end-to-end (BRAIN-02).
- Hub Devices and Settings copy reflect BrainFlow as first-class; discover/connect/disconnect flow unchanged and works for BrainFlow (BRAIN-03).
</success_criteria>

<output>
After completion, create `.planning/phases/09-brainflow-first-class/09-02-SUMMARY.md`
</output>
