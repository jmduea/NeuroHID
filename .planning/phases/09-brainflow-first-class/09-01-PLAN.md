---
phase: 09-brainflow-first-class
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/neurohid-core/Cargo.toml
  - docs/brainflow.md
  - docs/index.md
autonomous: true
requirements:
  - BRAIN-01
  - BRAIN-04
user_setup: []

must_haves:
  truths:
    - "User or developer can read first-class documentation for using BrainFlow with NeuroHID (setup, config, synthetic vs native, build order)"
    - "BrainFlow remains one backend behind DeviceProvider/Device; device-agnostic API is preserved"
  artifacts:
    - path: docs/brainflow.md
      provides: "Canonical BrainFlow doc (setup, config, synthetic vs native, build order)"
      min_lines: 40
    - path: crates/neurohid-core/Cargo.toml
      provides: "Default features include brainflow so Hub and examples work without custom build"
      contains: 'default = ["device-lsl", "brainflow"]'
  key_links:
    - from: docs/index.md
      to: docs/brainflow.md
      via: "Link in Architecture/System or User guide section"
      pattern: "brainflow"
---

<objective>
Enable BrainFlow in the default build path and add first-class documentation so users and developers can use BrainFlow with NeuroHID without custom build flags, and have a single reference for setup, config, synthetic vs native, and build order (Phase 10). Document that the device-agnostic API is preserved (BrainFlow is one backend).
Purpose: BRAIN-01 (first-class docs), BRAIN-04 (preserve abstraction — documented, no API change).
Output: neurohid-core default features include brainflow; docs/brainflow.md; docs/index.md link.
</objective>

<execution_context>
@C:/Users/spark/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/spark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-brainflow-first-class/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable BrainFlow in default build</name>
  <files>crates/neurohid-core/Cargo.toml</files>
  <action>
    Add "brainflow" to neurohid-core default features so that "device-lsl" and "brainflow" are both enabled by default. Current line: `default = ["device-lsl"]`. Change to: `default = ["device-lsl", "brainflow"]`. This ensures the Hub binary (which depends on neurohid-core via neurohid-hub) and examples that use neurohid-core get BrainFlow without requiring `--features brainflow`. Do not change neurohid-device or neurohid-sdk feature sets; neurohid-core is the crate that create_provider lives in and is the dependency used by the Hub.
  </action>
  <verify>cargo build -p neurohid-core succeeds; cargo build -p neurohid succeeds without extra features; grep -E "default.*brainflow" crates/neurohid-core/Cargo.toml</verify>
  <done>neurohid-core default features include brainflow; cargo build -p neurohid succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: First-class BrainFlow documentation</name>
  <files>docs/brainflow.md, docs/index.md</files>
  <action>
    Create docs/brainflow.md as the canonical doc for using BrainFlow with NeuroHID. Include: (1) Setup — enable BrainFlow (note: included in default build for Hub/examples; optional feature only if building neurohid-core without default). (2) Config — backend = BrainFlow, board_id (default 0 = synthetic), optional serial_port; reference neurohid-types BrainFlowConfig. (3) Synthetic vs native — current implementation is simulation/synthetic only (Phase 9); native SDK and real hardware are Phase 10 (BRAIN-06–08). (4) Build order — state that Phase 9 requires no C++ build; build order for native SDK is documented in Phase 10. (5) One sentence: BrainFlow is one backend behind the existing DeviceProvider/Device abstraction; device-agnostic API is preserved (BRAIN-04). Keep doc concise; link to docs/formats/stream-semantics.md for stream semantics if relevant. Then add a link to docs/brainflow.md from docs/index.md in an appropriate section (e.g. Architecture and System Docs, or a new "Device backends" / "BrainFlow" bullet).
  </action>
  <verify>File docs/brainflow.md exists with setup, config, synthetic vs native, build order, and API-preservation note; docs/index.md contains a link to brainflow.md; cargo build -p neurohid still succeeds.</verify>
  <done>First-class BrainFlow doc exists and is linked from index; readers can find setup, config, synthetic vs native, and build-order guidance.</done>
</task>

</tasks>

<verification>
- cargo build -p neurohid
- cargo build -p neurohid-core
- docs/brainflow.md exists and covers setup, config, synthetic vs native, build order, API preservation
- docs/index.md links to docs/brainflow.md
</verification>

<success_criteria>
- neurohid-core default features include brainflow; Hub can select BrainFlow in Settings without custom build.
- Single canonical BrainFlow doc exists and is linked from docs index; BRAIN-04 documented (no API change).
</success_criteria>

<output>
After completion, create `.planning/phases/09-brainflow-first-class/09-01-SUMMARY.md`
</output>
